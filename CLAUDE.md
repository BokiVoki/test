# 프로젝트 메모리 — 희은의 개인 시스템

> 이 문서는 "항상 기억하는 뇌"예요. 새 Claude 세션은 이 파일을 자동으로 읽어요.
> 무언가 바뀌면 이 문서도 같이 업데이트해요.

## 한눈에 — 지금 뭐가 돌아가고 있나

| 시스템 | 무엇 | 어디 사는가 | 상태 |
|---|---|---|---|
| **일정 비서봇** | 텔레그램 봇. 할일/알람/리마인더/아카이브/생리주기/구글캘린더 | Railway (BOT_ROLE 미설정) + Google Sheets | ✅ 운영 중 |
| **인박스봇** | 텔레그램 봇. 링크/생각/사진/쇼핑/책을 옵시디언 볼트로 자동 정리 | Railway (BOT_ROLE=inbox) + GitHub 볼트 `BokiVoki/obsidian-vault`(Private) | ✅ 운영 중 (Books/Inbox/Shopping 폴더에 커밋 중). 남은 건 옵시디언 앱에서 볼트 열기(Obsidian Git) |
| **하루(Haru) 웹앱** | 개인 업무 할일 대시보드 (미니멀·딥그린) + 그림공부(작가별 그림 태그 상속) | **Cloudflare** + Supabase, 그림은 옵시디언에도 미러링 | ✅ 운영 중 (`webapp/index.html`, `haru.lolcv1294.workers.dev`) |

---

## 1. 일정 비서봇 (`bot/`)

- **엔트리**: `python start.py` → `BOT_ROLE` 미설정이면 `bot.main` 실행
- **저장소**: Google Sheets (`SPREADSHEET_ID`) — Archive / Todos / Reminders / Cycle 워크시트
- **LLM**: Anthropic API (`ANTHROPIC_API_KEY`) — Haiku로 자연어 파싱
- **주요 기능**
  - 투두/알람: 자연어로 추가, 반복(daily/weekly/monthly/after:N분), 재알람, 완료
  - 아카이브: 책/웹툰/영화 등 기록, `/검색` 인라인 버튼, 사진 메모(Drive)
  - 생리주기 추적 + 단계별 알림(PMS 등)
  - **하루앱 주머니 던지기**: `앱 <내용> [마감]` → 하루 웹앱 주머니(Supabase todos, `bucket='inbox'`)로 바로 저장 (`bot/haru_app.py`). 끝에 붙은 날짜(오늘/내일/모레/글피, 요일, `M/D`·`M.D`·`M-D`, `M월 D일`)를 `parse_pocket_due`로 떼어 due로 저장. 환경변수: `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `HARU_OWNER_ID`, (선택 `HARU_WORKSPACE`, 기본 '일')
  - **구글 캘린더 일정**: 끝에 **`구캘`=캘린더만**, **`구할`=캘린더+하루앱 둘 다**(`_create_schedule(also_app)`), 앞에 `일정 `=캘린더만. 예: `출장 8/6-8/8 구캘`, `회의 8/6 구할` (`bot/gcal.py`, `parse_schedule`). 기간 `8/6-8/8`·`8월6일-8월8일`, 단일 `8/6`/`8월 3일`/오늘·내일, 시간 `8/6 14:00`(1시간). 서비스계정(GOOGLE_CREDENTIALS_JSON) 재사용 — **셋업**: Calendar API 사용설정 + 내 캘린더를 서비스계정 client_email에 '일정 변경'으로 공유 + env `GOOGLE_CALENDAR_ID`(기본캘린더면 내 gmail). **캘린더 + 하루앱 동시**: 성공 시 `haru_app.add_to_pocket(bucket='inbox', due=시작일, endd=종료일)`로 하루앱 주머니+캘린더에도 표시(기간이면 제목에 `~` + `todos.endd`로 캘린더에 시작~종료 쫙). 실패/미연결이면 답장에 이유 표시.
  - **하루앱 11시 체크인**: 매일 11:00(KST) 전용 메시지로 오늘 추천(우선순위순 `haru_app.list_open`)+마감(`list_due`)을 쏨 (`send_haru_daily`, `scheduler.send_haru_daily_job`, main.py UTC 02:00). 즉시 확인: `추천`/`체크인` 텍스트. 마감만: `마감` 텍스트 또는 `/deadlines`. (브리핑엔 합치지 않음)
- **최근 수정/버그픽스**
  - `cancel_alarms` 오발동 방지 (프롬프트 강화)
  - "모든 알람 취소"에 되돌리기 버튼 추가
  - 체크인 기능 **삭제됨**
  - 날짜/요일 파싱 정확도 개선 (요일 문자열 전달)
  - PMS 단계 알림 중복 발송 버그 → Cycle 시트 note에 `[notified:...]` 영구 기록으로 해결
  - **안 쓰는 기능 정리(2026-09-25)**: "요새 알림과 생리기록/배란기 정보 정도만 쓰고, 영양제 기능도 거의 안 쓴다"는 확인을 받고, 실제로 쓰는 기능(알림, 봇 자체 투두, 아카이브, 생리주기, 구글캘린더 연동, 하루앱 주머니 던지기)만 남기고 셋을 완전히 제거함:
    - **영양제 재고+복용기록** — `/inventory` `/intake` `/setup_supplements` 명령, 자연어 라우팅, 알람완료·뽀모도로 완료 시 자동 복용기록 side-effect, `bot/inventory_sheet.py`/`bot/intake_sheet.py`(파일 삭제), `models.py`의 `InventoryItem`/`IntakeLogItem`. `cycle_sheet.py`의 `format_status()`가 영양제 재고 기반 추천을 보여주던 것도 함께 제거하고 하드코딩된 안내 문구로만 표시하게 단순화(파라미터 자체를 없앰).
    - **봇 자체 메모** — `/memos` `/memo_del`, "기록해줘" 자연어 저장, `bot/memos_sheet.py`(파일 삭제), `models.py`의 `MemoEntry`. ⚠️ 하루앱의 메모 탭(리치 에디터)과는 완전히 다른, 훨씬 단순한 별개 기능이었음 — 하루앱 메모는 전혀 안 건드림.
    - **브리핑(아침 9시/저녁 6시/밤 11시 하루 3번 요약)** — 날씨+봇 자체 투두 요약으로 만들던 옛 기능. `send_briefing`/`send_briefing_job`/`_fetch_weather` 제거, `main.py`의 3개 `run_daily` 스케줄 제거. **하루앱 11시 체크인(`send_haru_daily_job`)은 완전히 별개 기능이라 안 건드림** — 그대로 유지.
    - 봇 자체 투두(`/todos` 등, Reminders와 같은 시트를 공유)와 아카이브·구글캘린더 연동·하루앱 주머니 던지기·웹 푸시·뽀모도로 기능 자체는 실제로 쓴다고 확인받아 손대지 않음.
  - **아카이브 → 하루앱 거울복사(2026-09-26)**: "일정봇 아카이브를 하루앱에 통합"이 결정되면서, Google Sheets(원본, 그대로 유지 — `/검색` 등 기존 기능 안 건드림)에 쓸 때마다 Supabase `archive` 표에도 같이 씀. 새 모듈 `bot/archive_app.py`(`haru_app.py`와 같은 패턴 — raw `requests`로 Supabase REST 호출, service key, 실패해도 조용히 삼켜서 시트 저장엔 영향 없음)를 만들고, `bot/sheets.py`의 `SheetsClient`(`add_entry`/`update_entry`/`delete_entry`/`batch_add_entries`/`batch_update_entries` 5개 메서드, 모든 아카이브 쓰기가 결국 이 5곳을 거침)에서 시트 저장 직후 `archive_app`을 호출하도록 함 — `handlers.py`의 호출부 15곳을 일일이 안 고치고 `sheets.py` 한 파일만 손대는 걸로 끝남(단일 choke point). **기존 기록(이미 시트에 있던 것들)은 자동으로 안 옮겨짐** — `/sync_archive_to_haru` 명령을 한 번 실행하면 시트 전체를 Supabase로 한 번에 복사(여러 번 눌러도 안전, id 기준 덮어쓰기). 이후로는 새로 추가/수정/삭제하는 게 자동으로 같이 반영됨. **환경변수는 하루앱 주머니 던지기와 완전히 동일**(`SUPABASE_URL`/`SUPABASE_SERVICE_KEY`/`HARU_OWNER_ID`, 이미 Railway에 있음 — 새로 넣을 것 없음). ⚠️ **단방향**: 하루앱 아카이브 탭에서 고친 내용은 시트로 다시 안 올라감(시트는 봇의 원본, 하루앱은 보고 고치는 화면) — 봇과 하루앱 양쪽에서 같은 항목을 동시에 고치면 나중에 저장한 쪽이 이김.

## 2. 인박스봇 (`inbox_bot/`)

- **엔트리**: `python start.py` + `BOT_ROLE=inbox` → `inbox_bot.main`
- **개념**: 명령어 없이 뭐든 던지면 자동으로 옵시디언 볼트(GitHub repo)에 `.md` 노트로 정리. 명령어일 때만 다른 처리.
- **동작**
  - 링크 → 본문 fetch + Claude(Sonnet) 요약 + "왜 저장" 한 줄. 인스타 등 못 읽는 사이트는 안내 문구
  - 생각/텍스트 → 아이디어 노트
  - 사진 → 볼트 `Inbox/attachments/`에 직접 커밋 + `![[...]]` 임베드. 저장 메시지에 **📝 글 인식** 버튼(`ocr_callback`, callback `o:{폴더}:{stamp}`) → 눌러야 실행(자동 아님): 노트 이미지를 볼트에서 `read_binary`로 다시 읽어 `capture.read_photo_text`(비전)로 글 추출 → 노트에 `## 📄 인식한 글` 추가 + **읽은 글에서 뽑은 태그/허브를 frontmatter에 합치고 그래프 링크(`관련: [[허브]]`)까지 옵시디언용 자동 정리**(`merge_tags_into_note`, 답장에 🏷/🗂 표시) + **읽은 글을 사용자에게 확인용으로 전송**(4096자 제한, 3500 넘으면 잘림). `_stampcb`/`_find_note` 공통.
  - 쇼핑("살까/얼마") → `Shopping/` 위시리스트 (비전으로 상품/가격 읽음)
  - 책("읽어볼까/책추천") → `Books/` 읽고싶은 책 (표지 제목/저자 읽음, 여러 권이면 목록)
  - `✍️ 내 생각` 버튼 → 사용자 말에서 태그 재추출, 캡션 우선
  - **삭제**: 각 저장 메시지에 `🗑 삭제` 버튼 — **재시작에도 안전하게** callback_data에 `d:{폴더코드}:{타임스탬프}`를 담음(`_del_button`/`delete_callback`, `list_folder`로 stamp 매칭 → 노트+`![[img]]` 첨부 삭제). 일괄: `삭제`/`삭제 N`/`최근삭제 N` 텍스트 → 인박스 최근 N개 삭제(`_delete_recent`). `vault.read_note`/`list_folder`/`delete_note`.
  - **사진 여러 장(앨범)**: `media_group_id`로 **file_id만 즉시 버퍼**(`_album_buf`, 핸들러는 업로드 안 함=빠름) → 4초 debounce 후 `_flush_album`이 다운로드+업로드하고 **한 노트에 전부 임베드**(`![[img]]` 여러 줄, 첫 캡션 사용). job_queue 없으면 개별 저장로 폴백. (예전에 핸들러에서 바로 업로드하다 느려서 앨범이 쪼개지던 것 → file_id 버퍼링으로 해결)
  - 명령어: `/today` `/find` `/shopping` `/books`
  - 비전 모델: `INBOX_VISION_MODEL` (기본 sonnet, opus로 올릴 수 있음)
  - **파일명 형식**: `제목 YYMMDD-HHMM.md` (제목 앞으로, 밑줄 X=띄어쓰기, 날짜는 뒤에 짧게) — 옵시디언 그래프에서 노드가 날짜 대신 **제목으로 읽히게** (`capture._slugify`는 공백 유지+금지문자만 제거, `capture._note_path`). 삭제/글인식 콜백은 파일명 뒤 `YYMMDD-HHMM` 스탬프로 노트 매칭(`_stampcb`/`_find_note`, `stamp in name`), 옛 형식(`YYYY-MM-DD_HHMM` 앞)도 호환. 최신순 정렬은 `vault._recency_key`가 파일명에서 날짜 뽑아 유지.
  - **허브를 실제 파일로**: `관련: [[허브]]`는 원래 유령 노드(파일 없음)라 옵시디언 그래프에서 색칠이 안 됨 → 노트 저장 시 `_save_note`(모든 write_note 래퍼)가 `_ensure_hub_notes`로 `Hubs/{허브}.md`(frontmatter `tags:[허브]`)를 자동 생성(`_hub_seen` 캐시). **`이름정리` 명령**이 기존 노트도 리네임 + 허브 백필(`_backfill_hubs`). **그래프 색칠은 옵시디언 앱에서 Groups `path:Hubs` → 초록**(코드 아님).
  - **태그 vs 허브 기준**: 태그(2~4개)=이 노트 하나의 짧은 속성/종류 라벨(예: 감상·인터뷰·유튜브·창의력), 허브(1개)=여러 노트가 재사용하는 넓은 카테고리(예: 글쓰기·브랜딩·자기계발) — 노트 요약이면 안 됨. **옵시디언 태그는 공백이 있으면 무효**라 프롬프트에 "공백 없는 한 단어"를 명시(모든 tags 프롬프트: `summarize`/`derive_tags`/`read_photo_text`/`parse_book`/`parse_shopping`) + 코드 안전장치 `capture._clean_tags`(공백 제거·중복 제거)를 모든 tags_yaml 생성 지점과 `merge_tags_into_note`에 적용(기존에 이미 오염된 태그도 다음 저장 때 자동 정리됨). **`태그정리` 명령**이 기존 노트 전체를 한 번에 정리(`_clean_note_tags`, 바뀔 것만 저장해서 빠름).
- **연결 상태**: ✅ 이미 연결됨. 볼트 = `BokiVoki/obsidian-vault`(Private), Railway 두 번째 서비스(`BOT_ROLE=inbox`)에서 커밋 중. env: `INBOX_BOT_TOKEN`, `VAULT_REPO`, `GITHUB_TOKEN`, `BOT_ROLE=inbox`, (공유: `ANTHROPIC_API_KEY`, `TELEGRAM_USER_ID`)
  - **남은 것**: 옵시디언 앱에서 그 볼트를 열어 보기 (GitHub Desktop으로 clone → 폴더를 볼트로 열기 → 자동 업데이트는 **Obsidian Git** 플러그인)

## 3. 하루(Haru) 웹앱 (`webapp/index.html`)

- **스택**: 단일 HTML 파일 + Supabase (DB+Auth) + Netlify 호스팅. 빌드 없음.
- **디자인**: 미니멀·샤프, 엑셀st 표, 라이트/다크. **포인트 컬러 — 스카이블루 + 버건디(2026-09-23, 호크니풍으로 교체)**: "키 컬러가 못생긴 초록 같다 · 스카이 블루와 버건디를 데이비드 호크니적으로"라는 요청으로 딥 포레스트 그린(`#2C6A46`)을 교체. `--accent`(주 포인트, 호크니 풀장st 시안블루) `#0E76AC`/dark `#5FB4E6` · `--accent-2`(버건디, 버튼 hover·캘린더 일정 글자색·토스트 "되돌리기" 링크에 씀) `#7A2743`/dark `#E58FA6`(다크에선 읽히게 로즈톤으로 밝힘) · `--accent-wash`(연한 하늘색 배경 틴트) `#E3F1F9`/dark `#10202B`. **토큰 3개(`--accent`/`--accent-2`/`--accent-wash`)가 `:root` 세 곳(기본·`prefers-color-scheme:dark`·`[data-theme]`)에만 있고 나머지 CSS는 전부 이 변수를 참조**하는 구조라 하드코딩된 초록 hex가 그 3곳 6줄 말고는 전혀 없었음 — 값만 바꿔서 앱 전체가 한 번에 재색칠됨(로직·레이아웃 변경 없음). `/cha`(찻잎 장부)는 하루앱과 의도적으로 분리된 페이지라 안 건드림.
- **디자인 2차 정리 — 박스·둥근모서리 걷어냄(2026-09-23)**: "다 아웃라인 있고 모서리 둥글어서 오히려 여백을 많이 만드는 느낌 · 검색처럼 밑줄 하나로도 되고 네모로 안 감싸도 됨"이라는 피드백. 스타일시트 전체를 훑어서:
  - **`border-radius`는 거의 다 제거**. 남긴 건 기능상 원형이어야 하는 것 셋뿐 — 사진 위 삭제·이동 원형 아이콘 버튼(`.mreadimg-del`/`.mreadimg-mv`), 손그림 색상 스와치(`.sk-color`). 체크박스(`.ck`/`.sck`)·태그 배지(`.rolled`/`.rtcyc`/`.art-tag`)·카드·팝업까지 전부 각지게.
  - **섹션을 박스로 감싸던 걸 걷어내고 구분선/여백만으로**: 홈 오른쪽 `.board`들(주머니·회의안건·링크·오늘한줄) → 테두리+배경 박스 대신 **보드 사이 위쪽 구분선 하나**로만(`.board{border-top}`, 첫 보드는 선 없음). 저널 `.jgrid`·메모 `.memo` 그리드 → 바깥 박스 없애고 원래 있던 **목록/본문 사이 세로줄**(`.jside`/`.mside`의 `border-right`)만 남김. 그림공부 카드(`.art-card`) → 테두리 없애고 이미지가 그대로 카드, hover는 살짝 밝기만.
  - **입력창은 검색창처럼 밑줄 하나로**: 검색(`.searchbar input`)이 기준 예시 — `border:0;border-bottom:1px solid`, `:focus`면 accent색. 같은 패턴을 담기(`.capture`)·서랍 링크 추가(`.linkadd input`)·로그인 폼(`.authcard input`)·작가 검색(`#artistSearch`)·그림공부 폼(`.art-fields input`/`.art-form input,textarea`)·메모 폴더 select(`.mfolsel`)까지 확장. **저널 본문(`.jbody`)·그림공부 공부메모(`.art-note`)는 아예 테두리 없이**(메모 본문 textarea `.mseg`와 같은 톤) — 이미 밑줄 스타일이던 `.pjaddin`/`.boardin`/`.prgin`(프로젝트 할일 추가·보드 입력·진행기록 입력)이 원래 이 방향의 선례였음.
  - **버튼은 얇은 테두리 유지**(`.btn-s`/`.monthnav button`/`.calnav button`/`.theme,.logout` 등, radius만 뺌) — 버튼 자체의 경계는 "박스로 감싸인 여백"이 아니라 클릭 가능함을 알려주는 제 역할이라 판단해서 남김. 모바일 「할일/보드」 세그먼트(`.homeseg`)도 필박스(배경 채움)에서 **상단 탭바(`.tabs`)와 같은 밑줄 인디케이터 방식**으로 다시 그림.
  - 라이트 테마에서 `--panel`이 `--bg`와 완전히 같은 흰색이라(둘 다 `#FFFFFF`), 박스 제거로 잃는 시각 정보는 원래도 테두리+radius뿐이었음(배경차는 다크 테마에서만 미세하게 있었음) — 그래서 박스를 없애도 라이트에서 내용이 사라지거나 붕 뜨는 느낌 없음.
  - 헤드리스 스텁으로 스크린샷 확인(홈/저널/메모/모바일 할일·보드 탭) + 기존 회귀 98개 전부 통과 — 이 변경은 CSS만 건드려서 기능 쪽은 원래도 안 건드릴 것으로 예상했지만 확인 차 전체 회귀를 돌림.
- **디자인 3차 — 오늘 영역 구분 + 주머니 정리(2026-09-23)**: "오늘의 집중/마감임박은 은은한 배경이라도 깔려서 아래 월별표랑 구분됐으면 좋겠다 · 주머니 체크박스랑 제목이 너무 멀다 · 오늘/날짜/프로젝트/나중/메모/버림 6개가 너무 많으니 눌러야 뾰로롱 나오는 식이면 좋겠다"는 요청.
  - **`.todayzone`**: 오늘의 집중 + 마감 임박 두 `.blk` 섹션을 `background:var(--sunk)` 배경 하나로 같이 감쌈(박스 테두리·radius 없이 면 색만) — 방금 걷어낸 "박스로 안 감싸기" 방향과 안 어긋나게, 테두리 대신 **배경 톤 차이**로만 그 아래 월별 표와 구획을 나눔.
  - **주머니 체크박스 간격**: `td.ckcell{width:30px;text-align:center}`가 원래 큰 표(칸 여러 개)용 기본값이라, 칸 하나(체크+제목)뿐인 좁은 사이드 보드에선 체크박스가 30px 칸 한가운데 떠서 제목과 멀어 보였음 → `.inboxtbl td.ckcell{width:auto;text-align:left}`로 좁혀서 제목에 바로 붙게 함.
  - **주머니 행 액션 6개 → `⋯` 눌러야 펼쳐지는 세로 메뉴로**: 오늘/날짜/프로젝트이동/나중/메모/버림이 행마다 늘 펼쳐져 있어서 시끄러웠던 걸, `renderInbox`에서 기본은 제목 옆 `⋯`(`.moretog`, `data-act="inb-more"`)만 보이게 하고, 누르면 **진행기록(`.prgrow`)·하위항목(`.subrow`)과 같은 패턴으로 그 아래 새 `<tr class="inbmorerow" data-id="…">`가 펼쳐지며** 6개를 세로 메뉴(`.tri.inbmenu{flex-direction:column}`)로 보여줌(`inbOpen` 세션 상태, `prgOpen`과 같은 패턴). **기존 액션 핸들러는 하나도 안 건드림** — `rowId()`가 `closest('tr[data-id]')`로 찾으므로, 펼침 행도 자기 `data-id`만 있으면 `t-today`/`due`/`t-proj`/`t-later`/`t-memo`/`t-del` 델리게이트 클릭·체인지 리스너가 그대로 동작함(별도 모달이나 새 핸들러를 안 만들어도 됨 — 이게 이 구현을 가볍게 만든 핵심).
- **디스플레이 폰트(2026-09-23)**: "앱 제목이랑 월별 제목이 좀 더 크고 재밌는 폰트였으면"이라는 요청 — 나머지 UI는 전부 시스템 산세리프(`--sans`) 그대로 두고, **딱 이 둘에만** 구글 폰트 **Do Hyeon**(`--display` 토큰)을 얹음: 헤더 `.brand`("하루", 22px, accent색) · 로그인 화면 `.authcard h1`("하루 🌿", 34px, accent색) · 홈 `.monthnav h2`("2026년 9월", 21px, accent색). `<head>`에 `fonts.googleapis.com` 링크 추가(`/cha`가 이미 같은 방식으로 Noto Serif KR을 쓰고 있어서 같은 패턴). 섹션 소제목(`.bhead h2` 등)이나 저널 날짜 제목(`.jhead h2`)은 요청 범위 밖이라 안 건드림 — 필요해지면 그때 넓히기.
- **탭(v2, 2026-09)**: **홈 / 저널 / 메모 / 캘린더 / 서랍 / 그림공부** — 예전의 `대시보드`+`주머니·프로젝트`가 **홈** 하나로 합쳐졌고, `완료함`은 **저널**로 흡수됨. 아래 "v2 재구조" 참고.
- **탭별 즐겨찾기(URL 해시 딥링크)**: 각 탭이 `#home`/`#journal`/`#memo`/`#cal`/`#drawer`/`#art` 해시를 가짐 — `switchTab`이 탭 바꿀 때마다 `history.replaceState`로 해시를 갱신해두고, 로드 시(그리고 `hashchange` 시) 그 해시가 유효한 탭이면 그 탭으로 바로 엶. 그래서 `haru.lolcv1294.workers.dev/#art`처럼 탭별로 따로 즐겨찾기 가능(예전엔 무조건 대시보드로 열렸음, `state.tab`이 `localStorage`에 저장 안 됐었음).
- **v2 재구조 (2026-09, 진행 중)**: 2026년 업무 엑셀(월별 시트 + 상시업무 + 작년 회고)에서 일하는 방식을 읽고 구조를 다시 짬. 와이어프레임: https://claude.ai/artifact/M88d4ZgDmCMXTbHPbDPvtL
  - **왜**: 엑셀이 하던 걸 앱이 못 하고 있었음 — ①한 화면 동시 조망(엑셀은 5~6개 영역을 가로로 펼쳐둠) ②업무가 달마다 이월(엑셀 6월 시트엔 `6월`·`7월` 체크 열이 나란히) ③할일 옆칸에 진행·회고 ④링크·문구·주의사항이 늘 옆에. "엑셀보다 비효율적"이라는 말의 실체가 이 넷.
  - **결정 사항**: 대분류(콘텐츠/도서/행정/스태프/매장정비)는 **프로젝트**로 둠 · 워크스페이스는 **안 나눔**(매장별 분리 안 함) · **메모 탭은 그대로 유지**(서랍에 흡수 안 함) · **서랍 = 홈에 자동으로 올라올 것들을 관리하는 곳**(상시업무 자동 복제 + 링크 핀 고정)
  - ⚠️ **엑셀 내용은 앱에 절대 넣지 말 것** — 포맷만 참고하고 데이터는 본인이 직접 채움. (와이어프레임 속 업무 이름들은 구조 설명용 예시일 뿐)
  - **1단계 완료**: 탭 재편 + 홈 화면(오늘 띠 + 월간 표 + 오른쪽 보드) + 월 이동 + 회고 칸 + 회의 안건 + 링크 보드
  - **2단계 완료**: 진행 기록(쌓임 + 저널 자동 연동) + 저널 본체(날짜별 기록, 월간 회고)
  - **3단계 완료**: 상시업무(`routines`) 마스터 + 매월 자동 복제 + 지난달 미완료 이월(`rolled`) — v2 재구조 끝
- **홈 화면(v2)**: `#home` 안이 `.homegrid`(본문 + 오른쪽 `.homeside` 보드, 900px 이하에선 세로로 쌓임).
  - **오늘 띠**: 기존 `오늘의 집중`·`마감 임박` 블록 그대로 (renderToday/renderDue 손 안 댐)
  - **월간 표**: `#monthTitle`+`‹ ›`+`이번 달로`(`#mThis`)로 달 이동(`goMonth`), 프로젝트 아코디언이 그 달 항목만 보여줌. 표 칸은 `체크 | 할일 | 진행사항 | 회고 | 마감 | 예상 | 중요`(7칸) — **회고**(`todos.review`)는 contenteditable이고 포커스 빠질 때 저장(예상시간 칸과 같은 방식). **진행사항·회고 둘 다 680px 이하에선 숨김**(`th:nth-child(3)`·`(4)` + `td.prgcell`·`td.rvwcell`) — 책상에서 쓰는 칸이라.
  - **월 귀속 규칙**: `tMonth(t)= t.month || thisMonth()` — **월이 안 정해진 예전 할일은 이번 달 것으로 쳐서 어디서도 안 사라지게** 함(백필 안 함). `rowT`가 저장할 때 `t.month`가 비어있으면 그 시점의 달로 채우므로, 기존 할일도 한 번 건드리면 자동으로 이번 달에 붙음. 새로 만들 땐 **보고 있는 달**로 들어감(`state.month`).
  - **꼭 이뤄야 할 것 · 이번 달 메모(2026-09-23, 2026-09-23 레이아웃 조정)**: 월별 제목(`#monthTitle`) 바로 아래 `.monthgoal` — 번호 매긴 "꼭 이뤄야 할 것" 3칸(`.mgmust`, `data-mi="0|1|2"`)이 **위**, 📝 이번 달 메모(순수 텍스트 인풋 `#mgGoal`, 처음엔 "월 목표"였다가 "이번달 메모"로 라벨만 바꿈 — 데이터 필드명 `goal`은 그대로)가 **아래**(DOM 순서만 바꾼 것, id·저장 로직은 그대로라 위치만 바뀜). **세로로 한 줄씩**(`.monthgoal{flex-direction:column}`) — 1·2·3이 각각 자기 줄 전체를 쓰고 그 아래 메모 한 줄(처음엔 가로로 나란히 두다 들쭉날쭉했던 걸 세로로 통일, 순서도 목표보다 할 일이 먼저 눈에 들어오게 조정). 저장은 **월간 회고와 같은 `journal` 행**(`date`='YYYY-MM', `jEntry(m)` 재사용)에 `goal`/`musts` 컬럼만 추가해서 같이 담음(마이그레이션 `add_journal_month_goal`, `hasMonthGoal` 프로브) — 굳이 새 테이블을 안 만들고 "그 달"을 나타내는 기존 journal 월간 행을 공유. 저널 탭의 월간 회고와 마찬가지로 **워크스페이스 구분 없이** 달 하나에 하나(journal 자체가 원래 ws 컬럼이 없어서 이 패턴을 그대로 따름). `renderMonthNav()`가 달 바뀔 때마다 `renderMonthGoal()`로 그 달 값을 다시 채워넣고(`state.month` 기준), 입력은 `jBody`와 같은 패턴으로 600ms 디바운스 + blur + pagehide/visibilitychange 시 즉시 저장(`mgSaveNow`). **입력 중엔 값을 덮어쓰지 않게**(`document.activeElement` 체크) 방어해둠.
  - **오른쪽 보드 4개**: **주머니**(프로젝트 없는 할일 = 기존 renderInbox 그대로, 280px에선 표 칸이 글자를 쪼개서 행을 flex로 풀어 [체크+제목] / [버튼들] 두 줄로 접음) · **회의 안건**(`agenda` 테이블 — 할일이 아니라 "회의 때 꺼낼 말"). 각 안건에 **담기**/**✕** — 안건이 정해지면 **담기**가 `AGENDA_PROJECT`(기본 `회의실행`) 프로젝트의 할일로 넘기고 안건 목록에선 뺌(중복 방지, 되돌리기 지원). 그 이름의 프로젝트가 없으면 담는 순간 자동으로 생김(`projectNames()`가 할일에서 뽑으므로 별도 생성 불필요). 처음엔 체크한 것을 한 번에 비우는 `정리` 버튼이었는데, 정해진 안건은 지우는 게 아니라 **할일로 넘어가야** 한다는 요청으로 바뀜. 체크(☐)는 "얘기 끝났지만 할 일은 아님" 표시로 남겨둠 · **링크**(`links`에서 `pinned`인 것만, ★는 서랍에서 꽂음) · **메모**(`memos.pinned`인 것만, 2026-09-23 추가 — "자주 보는 링크뿐 아니라 자주 보는 메모도 고정해두고 싶다"는 요청)
  - **검색 이동**: 찾은 할일이 다른 달에 있으면 `state.month`를 그 달로 옮긴 뒤 홈으로 보냄(안 그러면 표에 안 떠서 검색이 헛돎). 보관된 건 저널로.
  - **탭 순서 저장 호환**: `applyTabOrder`가 예전 이름(`dash`/`proj`/`done`)이 섞인 저장값을 만나면 그 순서를 버리고 기본 순서로 감(일부만 재배치돼 순서가 엉키던 것 방지).
- **진행 기록(v2 2단계)**: `progress` 테이블(`todo_id`·`date`·`text`) — 엑셀 진행사항 칸은 덮어쓰기라 이전 내용이 사라졌는데, 여기선 **날짜가 붙어 쌓임**. 표 칸(`prgCellHtml`)엔 **최신 한 줄 + `+N`**만 보이고, 누르면 그 아래로 전체 기록 행이 펼쳐짐(`prgRowHtml`, `prgOpen` 세션 상태 — `expanded`와 같은 패턴). 맨 아래 입력창에 Enter로 오늘 날짜 기록 추가, 각 줄 ✕로 삭제(되돌리기 지원). 한 번 쓰면 **그날 저널에도 자동으로 뜸**(저널이 `progress`를 직접 읽어서 — 따로 복사 저장 안 함).
- **저널(v2 2단계)**: `journal` 테이블(`date`·`body`). 왼쪽 날짜 목록 + 오른쪽 본문.
  - **날짜 목록**(`jAllDates`)은 ①완료 시점(`doneAt`) ②진행 기록 날짜 ③본문이 있는 저널 날짜 ④오늘 을 합쳐서 최신순. 목록 끝에 **월간 회고**(`date`가 `YYYY-MM`) 칸이 붙음.
  - **위는 자동, 아래는 자유**: 위 `#jAuto`에 그날 ✓체크한 할일 + ✎그날 쓴 진행 기록이 자동으로 깔리고(읽기 전용), 아래 `#jBody`에 자유롭게 씀.
  - **월간 회고 = 그 달 일지 전체 보기**: 완료 개수·진행 건수 요약 밑에 **그 달 하루하루가 날짜순으로 쭉**(`.jdig`) — 각 날짜에 `·`저널 줄 + `✓`완료 할일 + `✎`진행 기록이 다 들어가고, 날짜를 누르면 그날 칸으로 이동. 길어지니 `.jauto.digest`에 `max-height:340px`+스크롤을 걸어서 아래 회고 입력칸이 화면 밖으로 밀리지 않게 함. (요약 숫자만 보여주던 걸 "매일의 기록을 한눈에" 요청으로 확장)
  - **홈 오른쪽 「오늘 한 줄」 보드**: 저널 탭까지 안 가도 홈에서 바로 그날 기록을 한 줄씩 붙이는 칸(`#todayLineIn`, `renderTodayLine`). **같은 `journal` 행을 저널 탭과 나눠 쓰는 구조** — 홈에서 Enter로 줄을 덧붙이고(덮어쓰지 않음), 저널 탭에서 고치면 홈에도 그대로 반영됨. 각 줄 ✕로 삭제(되돌리기 지원). 보드 순서는 **오늘 한 줄 → 주머니 → 회의 안건 → 링크 → 메모**.
  - **📌 메모 고정 보드(2026-09-23)**: 링크처럼 메모도 자주 보는 것만 홈에 꽂아두고 싶다는 요청. `memos.pinned bool`(마이그레이션 `add_memo_pinned`, `hasMemoPin` 프로브) 추가. 메모 목록(`#mlist`)의 각 `.mitem` 제목 옆에 ☆/★(`.mpin`, `mlist`의 로컬 클릭 리스너에서 처리 — 다른 델리게이트와 안 겹치게 클릭이 `.mitem` 열기로 안 번지도록 별도 분기) 누르면 토글. 홈 보드(`#memoPins`, `renderMemoPins`)는 `링크` 보드와 같은 `.lkrow` 스타일 재사용 — 제목(`.mktxt`) 클릭하면 메모 탭으로 이동+그 메모가 바로 열림(`state.curMemo` 설정, 검색 결과 클릭과 같은 패턴), ★(`.lkstar`) 클릭하면 그 자리에서 고정 해제(전역 `data-act` 델리게이트의 `mk-pin`/`mk-open` 분기, `data-mk` 속성으로 메모 id 참조 — 할일 행의 `rowId()`/`tr[data-id]` 패턴과 달라서 별도 분기로 처리). `renderMemos()` 끝에서 매번 `renderMemoPins()`도 같이 갱신.
  - ⚠️ 본문은 **메모 에디터가 아니라 순수 `<textarea>`** — 메모 에디터(세그먼트 트리·자체 undo·사진)는 `state.curMemo`/`#mbodyTree`에 강하게 묶여 있어서 저널에 재사용하려면 대규모 리팩터가 필요하고, 그게 메모 기능을 깨뜨릴 위험이 커서 일부러 단순하게 감. 불릿·사진이 필요해지면 그때 결정.
  - **저장**: 입력 600ms 디바운스 + 포커스 아웃 + **날짜 옮기기 직전**(`jSaveNow`) + `pagehide`/`visibilitychange` — /cha에서 겪은 "저장했는데 날아감"을 반복하지 않으려고 빠져나가는 길목마다 저장함.
  - **완료함은 저널 탭 아래쪽에 그대로** 남아있음(`doneList`, 되돌리기 포함) — 탭이 없어졌다고 기능을 잃지 않게.
- **상시업무(v2 3단계)**: `routines` 테이블(`project`·`title`·`cycle`·`active`). 서랍에서 `대분류 + 업무 이름 + 주기`로 등록하고 대분류별로 묶여 보임(`renderRoutines`).
  - **주기는 원래 매월 / 두 달에 한 번 두 가지만이었고, 매일·매주 반복은 `🔁 고정업무`(`repeat`)가 따로 담당했으나, 2026-09-25에 그 `🔁 고정업무`를 여기로 완전히 흡수**하면서 매일/며칠·몇 주·몇 달마다/특정 요일마다까지 5가지로 늘어남(아래 "상시업무 ↔ 고정업무 통합" 참고). 격월은 **등록한 달(`created`)을 기준**으로 한 달 걸러(`routineDueIn`).
  - **복제**(`seedRoutines`): 그 달에 같은 `routine_id` 할일이 이미 있으면 건너뜀 → **여러 번 눌러도 안 늘어남**. 홈 월 네비의 `＋ 상시업무`(`#seedBtn`)로 **보고 있는 달**에 수동으로도 담을 수 있음.
  - **이월**(`rollOver`): 지난 달(들)의 **미완료 + 프로젝트 있는** 할일을 이번 달로 **옮김**(복사 아님 — 한 행이 한 달에만 속하므로). 주머니(프로젝트 없음)는 달 개념이 없어서 건드리지 않고, 완료·보관된 건 그대로 그 달에 남음.
  - **언제 도는지**(`monthlyRollIfNeeded`): `settings.routineMonth`가 이번 달과 다르면 로드 시 1회 — `todayResetDate`와 같은 패턴. ⚠️ **이 기능을 처음 쓰는 로드에선 이월을 건너뜀**(`first`) — 안 그러면 예전 할일이 무더기로 이번 달로 끌려옴. `putSettings()`가 `state.uid` 없으면 저장을 건너뛰므로 uid가 아직이면 800ms 뒤 한 번 재시도(안 그러면 다음 로드에 또 돌아서 토스트가 반복됨).
  - **배지**: 이월된 건 `↩︎이월`, 상시업무에서 온 건 `상시`(`.rolled` 스타일 공용).
  - **멈춤/삭제**: `켜짐/멈춤` 토글은 다음 달부터 안 올라오게만 하고, **이미 담긴 이번 달 할일은 그대로 둠**(삭제도 동일) — 마스터를 건드려서 진행 중인 달이 흔들리지 않게.
- **서랍(v2)**: `🔔 알림`(웹 푸시, 아래 참고) + `링크`(전체 목록 + ★로 홈 고정) + `상시업무` 세 칸.
- **🔔 아이폰 푸시 알림(2026-09-24, 기반만 — 트리거는 나중에)**: "아이폰 푸시알림도 넣어줄아"라는 요청. 잠금화면 위젯("투두 목록을 위젯처럼")은 **불가능**하다고 먼저 안내함 — WidgetKit은 네이티브 앱 전용이라 웹앱(PWA)은 위젯 시스템 자체에 접근할 방법이 없음(콘텐츠가 할일이든 명언이든 동일하게 막힘). 대신 웹 푸시(Web Push)는 가능해서 그 기반만 먼저 구현.
  - **아이폰 제약**: iOS Safari는 **"홈 화면에 추가"로 설치한 PWA에서만** 웹 푸시 수신 가능(iOS 16.4+, 사파리 탭으로 그냥 열어둔 상태로는 원천 차단, 우회 불가). 그래서 `webapp/manifest.json`(설치용 매니페스트, 기존 `apple-touch-icon.png`180×180 + `favicon-512.png`512×512 재사용, 새 이미지 생성 안 함) + `<head>`에 `apple-mobile-web-app-capable` 등 메타 태그 추가.
  - **서비스워커**(`webapp/sw.js`): `push` 이벤트에 `showNotification`, `notificationclick`에 해당 URL로 포커스/열기. 오프라인 캐싱은 안 함(항상 최신 Supabase 데이터를 봐야 해서).
  - **구독 UI**(서랍 `🔔 알림`, `#pushStatus`/`#pushHint`/`#pushToggle`): `renderPushUI()`가 상태별로 다르게 보여줌 — ①`hasPush` 프로브 없으면 "마이그레이션 필요" ②`pushSupported()`(serviceWorker+PushManager+Notification) 없으면 "지원 안 됨" ③**아이폰인데(`pushIsIOS()`) 아직 설치 안 했으면**(`pushStandalone()`) "설치 필요"+버튼 숨김 ④그 외(데스크톱/안드로이드는 설치 없이도 되므로 아이폰만 이 게이트를 탐) 실제 구독 여부 확인해서 켜짐/꺼짐 버튼. **처음엔 이 아이폰 전용 게이트를 전체에 걸어서 데스크톱 크롬에서도 "설치 필요"만 뜨는 버그**가 있었음(헤드리스 테스트로 발견) → `pushIsIOS()`(UA + 아이패드가 Mac으로 위장하는 것 대응 `navigator.platform==='MacIntel'&&maxTouchPoints>1`)로 아이폰/아이패드에만 그 제약을 걸도록 수정.
  - **구독 저장**(`subscribePush()`): `Notification.requestPermission()` → `pushManager.subscribe()` → 구독 정보(`endpoint`/`p256dh`/`auth`)를 Supabase `push_subscriptions`(신규 테이블, `owner uuid`+RLS)에 upsert(PK=`endpoint`라 같은 기기 중복 구독 방지, 기기 여러 대 등록 가능). `unsubscribePush()`는 반대로 구독 해제+DB에서 삭제.
  - **VAPID 키**: 이 세션에서 1회 생성해서 고정(파이썬 `cryptography` 패키지가 샌드박스에서 깨져있어서 P-256 타원곡선 스칼라 곱셈을 직접 순수 파이썬으로 구현해서 생성, 커브 위에 있는 점인지 검증까지 함). 공개키는 `webapp/index.html`의 `VAPID_PUBLIC_KEY` 상수에, 비밀키는 Railway 환경변수 `VAPID_PRIVATE_KEY`에만 넣음(코드/문서에 노출 안 함) — **하나만 바꾸면 기존 구독이 다 깨지므로 재생성 금지**.
  - **발송 쪽**(`bot/webpush_notify.py`, Railway 텔레그램 봇에서 담당 — 발송은 서버가 있어야 해서 웹앱 혼자서는 못 함): `haru_app.py`와 같은 방식으로 Supabase REST를 raw `requests`로 호출해 그 사용자의 구독 목록을 가져온 뒤 `pywebpush`로 하나씩 발송, 만료된 구독(410/404)은 조용히 DB에서 지움. `requirements.txt`에 `pywebpush==2.0.3` 추가. **텔레그램 `푸시테스트`** 명령으로 등록된 기기에 테스트 알림 즉시 발송(핸들러는 `마감`/`추천`과 같은 텍스트 매칭 패턴).
  - **트리거는 아직 안 만듦**(사용자가 "일단 기반만" 요청) — 나중에 마감 임박/아침 체크인/회의 안건 등 중 골라서 `webpush_notify.send(title, body, url)` 호출만 연결하면 됨. 이미 저장 중인 이번 달 메모(`journal.goal`)·명언 같은 것도 나중에 알림 내용에 얹을 수 있음.
  - **테스트 한계**: 헤드리스 샌드박스는 `file://`에서 서비스워커 등록 자체가 막혀 있어(Chromium이 file: origin을 지원 안 함) 로컬 HTTP 서버(`python -m http.server`)를 임시로 띄워서 등록·구독 버튼 클릭까지는 확인했지만, **실제 구글 푸시 서버로의 등록은 샌드박스 네트워크에서 막혀 실패**(`Registration failed`) — 버튼 동작·에러 처리까지는 검증됐고, 실제 수신 여부는 배포 후 진짜 기기(아이폰 설치 후, 또는 데스크톱 크롬)에서 직접 "🔔 알림 켜기" → 텔레그램 `푸시테스트`로 확인해야 함.
  - **DB**: `push_subscriptions`(`endpoint` PK, `owner`, `p256dh`, `auth`, `ua`, `created`) 신규 테이블, RLS `owner=auth.uid()`.
  - **다음 단계로 예고된 것**: 푸시 마무리되면 **위젯 전용 미니 네이티브 앱**(Swift/WidgetKit, 잠금화면 위젯) 착수 예정 — 앱 전체를 새로 만드는 게 아니라 위젯만 담당하는 작은 앱. 이 세션(리눅스 서버)엔 Xcode가 없어서 **코드는 짜줄 수 있어도 빌드·테스트·앱스토어 업로드는 맥+Xcode에서 직접 해야** 함. 잠금화면 위젯 크기(`accessoryRectangular`, 가로 ~160pt) 자체는 매우 작아서 원래 "체크박스 몇 개 정도 압축해서" 수준으로 안내했는데, 사용자가 참고 앱 **ViewDo**(앱스토어 링크 공유, 이 세션 네트워크에서 apps.apple.com 접근이 막혀 있어 직접 확인은 못 함)를 근거로 "더 많이 체크 가능하게"를 요청 — iOS 17+ 인터랙티브 위젯(App Intents 기반 Button)은 애플 리마인더 위젯처럼 짧은 항목 여러 개+체크가 가능하다는 걸 재확인하고 방향을 그쪽으로 수정. 위젯으로 보여줄 데이터도 기존 할일/오늘의 집중이 아니라 **완전히 새로운 "루틴" 섹션**을 쓰기로 함(아래 항목).
- **캘린더 탭 재구조 + 워크스페이스 제거 (2026-09-24, 진행 중 — 1단계 완료)**: 잠금화면 위젯에 띄울 데이터를 기존 할일 체계가 아니라 **완전히 새로 구상한 "루틴"·"투두" 섹션**으로 쓰기로 결정하면서 시작된 재구조. 3단계로 나눠 진행 예정(①탭 뼈대 ②루틴 엔진 ③투두+위젯 연동).
  - **결정 사항**: 워크스페이스(`일`/`개인작업`) **완전 제거** — 두 개로 나눠 쓰던 게 오히려 헷갈려서 하나로 통합. 캘린더 탭 안에 **루틴 / 투두** 두 서브탭 추가(기존 달력 그리드는 `달력` 서브탭으로 그대로 유지). 루틴은 **자유 주기**(N일/N주/N달 + 요일·특정 날짜 지정)로 체크하면 다음 항목이 그 주기 뒤에 새로 생기고 그 전엔 뒤로 밀리는 방식(습관 트래커에 가까움), 투두는 캘린더에 안 뜨는 **단순 체크리스트**(초기화 없음, 위젯에서 체크+새 항목 추가 시 앱 빠른입력 화면으로 이동 — 위젯 자체엔 텍스트 입력창을 못 넣는 iOS 제약 때문).
  - **1단계 완료**: **워크스페이스 제거** — 기존 `개인작업` 소속 할일 5개(책읽기·발목강화 운동·탈잉 듣기·그림그리기 관련 등, 전부 실제 데이터)를 삭제하지 않고 SQL로 `ws='일'`로 마이그레이션한 뒤(`update public.todos set ws='일' where ws='개인작업'`), 헤더의 `#wsSeg`(일/개인작업 스위처) UI를 완전히 제거. `state.ws`는 코드에 그대로 남아있지만(다른 곳 전부 `t.ws===state.ws`로 필터링해서 참조 범위가 넓어 안전하게 남겨둠, 굳이 걷어낼 필요 없음) 이제 전환할 스위처가 없으니 항상 기본값 `'일'`로 고정 — 사실상 모든 코드가 한 워크스페이스만 보게 됨. 죽은 코드(`.seg` CSS, `#wsSeg` 관련 클릭 핸들러)도 같이 정리.
  - **캘린더 서브탭 뼈대**: `#calSubtabs`(`.subtabs`, 메인 상단 탭 `.tabs`와 완전히 분리된 별도 클래스+`data-caltab` 속성 — 메인 탭의 `document.querySelectorAll('.tabs button')` 델리게이트가 실수로 얘도 같이 집어가서 `switchTab(undefined)`를 호출해버리는 충돌을 피하려고 일부러 다른 이름 씀) — `달력`(`#calGridPanel`, 기존 그리드 그대로) / `루틴`(`#calRoutinePanel`) / `투두`(`#calTodoPanel`, 아직 안내 문구만 — 3단계에서 구현) 세 패널을 토글.
  - **2단계 완료 — 루틴 엔진**: 새 테이블 `cal_routines`(`hasCalRoutines` 프로브) — 기존 `routines`(상시업무, 매월/격월+프로젝트 전용)와는 완전히 별개. 주기 종류(`kind`) 4가지: `daily`(매일, 자정 자동 리셋) · `interval`(N일/N주/N달마다, `unit`+`n`) · `weekday`(특정 요일들마다, `weekdays` 배열) · `monthday`(매달 특정일, `monthday`). **핵심 동작**: `daily`는 체크해도 오늘 목록에 그대로 남아 켜짐/꺼짐만 토글(`doneToday`, `dailyResetToday()`가 매일 자정 다 꺼줌 — 기존 "오늘" 플래그 리셋과 같은 함수에 얹음). 나머지 세 종류는 체크하는 순간 **오늘 목록에서 사라지고**(`habitDueToday`가 `nextDue<=오늘`일 때만 보여줌) `habitNextDue()`가 계산한 다음 예정일까지 "예정된 루틴" 목록으로 내려감(안 그러면 완전히 숨어서 관리를 못 하니 두 목록으로 나눔) — 처음 만든 루틴은 `nextDue`가 비어있어서 바로 오늘 목록에 뜸. `weekday`는 오늘 다음으로 가장 가까운 지정 요일, `monthday`는 다음 달 같은 날짜, `interval`은 체크한 날+주기만큼 뒤. 폼(`.crform`)이 `kind` select 값에 따라 관련 입력칸만 보여줌(`crIntervalFields`/`crWeekdayFields`/`crMonthdayFields`). 날짜 계산은 헤드리스 테스트에서 **`Date`를 오버라이드해 "오늘"을 고정**하고(화요일 기준 월/목 지정→다음 목요일, 9/22 기준 매달 15일→10/15, 2주마다→10/6 등) 검증. 이때 `.ck.on`(체크됨 표시) CSS가 원래 테이블 안(`tr.done .ck`)에만 있어서 이 div 기반 행에선 체크해도 **눈으로는 안 보이는 잠재 버그**가 있었음(클래스는 붙지만 색이 안 바뀜, 클래스 존재만 검사하던 테스트라 못 잡았었음) → `.ck.on{}` 규칙을 테이블 밖에도 추가해서 고침(2단계·3단계 체크박스 둘 다에 적용됨).
  - **3단계 완료 — 투두 체크리스트 + 잠금화면/홈 화면 위젯**: 새 테이블 `cal_todos`(`hasCalTodos` 프로브, `id`/`title`/`done`/`sort`/`created`) — 캘린더에 안 뜨고, 초기화도 없는 **단순 체크리스트**(기존 `todos`의 프로젝트·마감·우선순위 체계와 완전히 무관, 그래서 새 테이블로 분리). 체크한 항목은 지워지지 않고 취소선 처리된 채 **목록 맨 아래로** 정렬(`(a.done-b.done)`). 삭제는 되돌리기 토스트 지원(다른 삭제와 동일 패턴).
  - **위젯 백엔드 — Supabase Edge Function `widget-data`**: 위젯(로그인 세션 없음)이 안전하게 읽고 체크할 수 있는 좁은 API. Supabase JWT 대신 **고정 토큰**(`x-widget-token` 헤더)으로만 인증 — 서비스 키를 Swift 앱에 직접 넣지 않으려고(그러면 앱을 리버스해서 DB 전체 키가 털릴 위험) 이 엔드포인트 하나에만 통하는 별도 토큰을 새로 생성해서 씀(DB 서비스 키와 무관, 이 토큰이 새도 블라스트 반경이 "루틴/투두 읽기·체크·투두 추가"로 좁음). `GET`으로 오늘 체크할 루틴(`habitDueToday` 로직을 TypeScript로 그대로 포팅)+안 끝난 투두를 반환, `POST {type,action,id/title}`로 체크(`routine`이면 `habitNextDue`도 TS로 포팅해서 다음 예정일 계산)·투두 추가. **하드코딩된 owner uuid**(개인 단일사용자 앱이라 `HARU_OWNER_ID`처럼 이 함수 안에도 고정값으로 넣음, `auth.users`에서 조회한 값). `verify_jwt:false`로 배포(커스텀 토큰 인증이라 Supabase 기본 JWT 검사는 끔). **이 세션 네트워크에서 supabase.co가 막혀 있어 직접 curl 테스트는 못 했음** — 코드 로직은 이미 헤드리스 테스트로 검증된 웹앱 JS의 `habitNextDue`/`habitDueToday`를 그대로 옮긴 것이라 로직 자체의 신뢰도는 높지만, 실제 호출은 iOS 기기에서 처음 확인하게 됨.
  - **Swift 위젯 코드**(`ios-widget/`, 새 최상위 폴더 — Xcode 프로젝트 파일(.xcodeproj)은 리눅스에서 못 만들어서 없음, Swift 소스만 커밋해두고 `ios-widget/README.md`에 Xcode에서 새 프로젝트+위젯 익스텐션 타겟 만들고 이 파일들 붙여넣는 순서를 적어둠): `HaruWidgetShared/`(양쪽 타겟이 같이 씀 — `WidgetModels.swift` Codable 모델, `WidgetAPI.swift` URLSession 네트워킹) · `HaruWidgetExtension/`(위젯 타겟 — `Provider.swift`가 `TimelineProvider`로 30분마다 갱신 시도, `CheckItemIntent.swift`가 iOS 17+ 인터랙티브 위젯의 체크 버튼 App Intent, `HaruWidgetView.swift`가 `accessoryRectangular`(잠금화면, 최대 4개 압축)·`systemMedium`(홈 화면, 최대 8개 여유있게) 두 크기 다 지원) · `HaruWidgetApp/`(메인 앱 타겟 — 화면이 **딱 하나**, 투두 빠른 추가 폼. 위젯에서 체크 버튼이 아닌 곳을 탭하면 `.widgetURL()`로 이 앱이 열리는데 화면이 하나뿐이라 자연스럽게 빠른입력 화면으로 연결됨, 별도 딥링크 라우팅 로직 불필요). **App Group 안 씀** — 위젯과 앱 둘 다 같은 Edge Function을 직접 HTTPS로 호출해서 데이터를 공유할 방법(공유 UserDefaults 등)이 필요 없었음, 그래서 Xcode 설정이 좀 더 단순함.
  - **테스트 한계**: 이 세션엔 Swift 컴파일러도 Xcode도 없어서 **문법 검증을 못 함** — iOS 17 App Intents/WidgetKit의 표준 API 패턴(`Button(intent:)`, `TimelineProvider`, `StaticConfiguration` 등)을 따라 신중하게 작성했지만, 실제 빌드·동작 확인은 맥+Xcode에서 처음 하게 됨. 무료 Apple ID로 기기에 설치하면 **인증서가 7일마다 만료**되니 그 안에 Xcode로 재실행해야 함(유료 개발자 계정 없으면).
  - **루틴/투두 개별 수정 + 매일 루틴 시간대 구분(2026-09-25)**: "루틴/투두 항목 개별 수정 가능하게, 매일 루틴은 아침/점심/저녁 구분, 매일 루틴/그 외 정기 루틴 파트 구분해서 표기"라는 요청.
    - **제목 인라인 수정**: 루틴·투두 행의 제목(`.mktxt`)을 `contenteditable="true"`로 바꿔서 클릭해서 바로 고칠 수 있게 함 — 월간 표 회고 칸(`.rvw`)·예상시간 칸(`.est`)이 이미 쓰던 "포커스 빠질 때 저장" 패턴을 그대로 재사용(`document.addEventListener('blur',...,true)` 델리게이트에 `cr-rename`/`ct-rename` 분기 추가, 빈 제목으로는 못 바꾸게 방어).
    - **루틴 주기 수정**: 각 루틴 행에 `✎`(`.crEditTog`, 모바일 터치 접근성 위해 기존 hover-only 아이콘들과 같은 680px 이하 `opacity:.5!important` 그룹에 포함) 누르면 그 아래로 주기 수정 박스(`.crEditBox`)가 펼쳐짐 — 새 루틴 등록 폼과 똑같은 필드 구성(주기 종류+아침/점심/저녁+며칠마다+요일+날짜)을 재사용, 이미 저장된 값으로 미리 채워서 보여줌. 박스 안에서 주기 종류를 바꾸면(아직 저장 전) 그 박스 안 필드만 즉시 바뀜(`.crEKind`의 `change`를 델리게이트로 잡아서 같은 `.crEditBox` 안의 `.ceSlot`/`.ceInterval`/`.ceWeekday`/`.ceMonthday`만 토글 — 상단 등록 폼의 `#crKind` change 핸들러와 로직은 같지만 특정 박스로 스코프를 좁힘). **저장하면 `nextDue`를 비워서**(`h.nextDue=''`) 새 주기 설정으로 바로 오늘 체크 대상이 되게 함(예전 주기 기준으로 미뤄둔 날짜가 새 규칙과 안 맞을 수 있어서 리셋).
    - **매일 루틴 시간대 구분**: `cal_routines.slot` 컬럼 추가(`''`|`morning`|`lunch`|`evening`, `hasRoutineSlot` 프로브) — `kind==='daily'`일 때만 의미 있음(등록 폼·수정 박스 둘 다 `kind`가 `daily`일 때만 시간대 select 보임). **매일 루틴 / 그 외 정기 루틴 / 예정된 루틴 세 섹션으로 완전히 분리**(전엔 "오늘 체크할 루틴" 하나에 매일+그외가 섞여 있었음) — `#calRoutineDaily`가 매일 루틴만 아침→점심→저녁→시간무관 순서로 소제목(`.crSlotHead`) 나눠서 보여주고(그룹 안에 항목 없으면 그 소제목 자체를 생략), `#calRoutineList`는 그 외 정기 루틴 중 오늘 체크할 것만, `#calRoutineUpcoming`은 그 외 정기 루틴 중 아직 주기가 안 돌아온 것만(매일 루틴은 항상 "오늘" 개념이라 예정 목록에 안 감).
    - 헤드리스 테스트로 슬롯별 그룹핑 순서, 섹션 분리, 인라인 제목 수정(DB 반영까지), 주기 수정 박스의 실시간 필드 전환+저장 후 DB 반영(interval→weekday 변경 케이스로 검증) 전부 확인.
    - **버그: 모바일에서 루틴 삭제가 잘 안 됨(같은 날 발견·수정)**: ✎(주기 수정) 아이콘을 새로 추가하면서 `.lkrow` 한 줄에 제목(contenteditable)+✎+✕ 세 개가 붙어 있게 됐는데, ✎/✕ 둘 다 글자 크기 11px에 여백 없는 아이콘이라 탭 가능 영역이 실제로 14px 안팎밖에 안 됐음(애플 권장 최소 44pt에 한참 못 미침) — 손가락으로 누르면 옆 아이콘이나 빈 자리를 짚기 쉬워서 "삭제가 잘 안 된다"로 느껴졌던 것. `.lkrow .lkdel,.lkrow .crEditTog{padding+음수 마진}`으로 시각적 크기는 그대로 두고 실제 탭 영역만 20~36px 수준으로 키움(680px 이하 모바일 전용). 헤드리스에서 **실제 터치 탭(`page.tap`)으로 삭제가 되는지**까지 검증(단순 `page.click`이 아니라 모바일 뷰포트+`hasTouch`로 재현).
- **상시업무 ↔ 고정업무(🔁) 통합, 대시보드 🔁 완전 제거(2026-09-25)**: 기능 중복 점검 요청으로 발견 — 업무 반복을 표현하는 시스템이 세 개(캘린더 탭 루틴=일상용, 서랍 상시업무=월/격월, 대시보드 행마다 붙이던 🔁 고정업무=매일/매주/매월)나 있었는데, 그중 **일상용 캘린더 루틴은 그대로 두고**(사용자가 명확히 "루틴은 일상, 상시업무/고정업무는 업무"라고 구분), **업무용 둘(상시업무·고정업무)만 서랍의 "상시업무" 하나로 합침**. 실제 데이터는 각각 1개뿐이라(`routines` 1개, `repeat` 걸린 할일 1개) 이 시점에 정리.
  - **상시업무 주기가 매월/격월 2개 → 5개로 확장**: 매월·격월(기존, project 필수, 매달 그 달 월간 표에 새 할일로 자동 복제)에 **매일·며칠/몇 주/몇 달마다·특정 요일마다**(신규, project 선택)를 추가 — 이 셋은 월간 표에 안 올라오고 서랍 화면 그 자리에서 바로 체크만 토글되는 방식(캘린더 루틴의 `habitNextDue`/`habitDueToday` 계산 엔진을 그대로 재사용, `routines` 테이블에 `unit`/`n`/`weekdays`/`next_due`/`done_today` 컬럼 추가 — 마이그레이션 `routines_toggle_kinds`, `hasRoutineToggle` 프로브. DB 컬럼명은 기존 `cycle`을 그대로 재사용해 5가지 값을 다 담음, JS에서는 `kind`로 읽음). `routineDueIn()`이 매일/주기/요일 종류는 애초에 월간 표 시딩 대상에서 걸러내므로(`ROUTINE_PERIODIC`/`ROUTINE_TOGGLE` 배열로 구분) 월간 표는 안 건드림.
  - **대시보드 🔁(고정업무) 완전 제거**: 할일 행마다 있던 🔁 버튼(`data-act="cad"`, daily/weekly/monthly 순환, `donelog` 완료 이력 카운트)과 관련 함수(`isRepeatDueToday`/`repLabel`/`repDoneCount`), "오늘의 집중"의 "고정 업무" 그룹, "완료함으로 정리"의 repeat 제외 조건을 모두 제거 — 이제 업무 반복은 서랍 상시업무 등록 하나로만 함. `todos.repeat`/`repday`/`lastdone`/`donelog` 컬럼 자체는 안 지움(스키마 변경 없이 항상 `'none'`/빈 값으로만 쓰이는 죽은 컬럼으로 남김, 데이터 손실 위험 있는 컬럼 드롭은 안 함). 참고로 `todos.donelog`는 애초에 실제 DB엔 컬럼이 생성된 적이 없어서(마이그레이션 누락) 고정업무의 "N회" 완료 카운트 기능은 원래도 동작한 적이 없었음.
  - **기존 데이터 마이그레이션**: 유일하게 있던 `repeat='monthly'` 할일("그림그리기 & 그림연구 공부", 프로젝트 "자아실현")을 SQL로 새 `routines` 행(매월, 같은 프로젝트)으로 만들고, 그 할일 자체는 `repeat='none'`+`routine_id`로 새 상시업무를 가리키게 연결(같은 달에 중복 시딩 안 되게 — 새 행을 또 만드는 대신 기존 할일을 그대로 이어받음).
  - 헤드리스 테스트(`routinemergetest.js`)로 매일 체크 토글·요일별 등록·N일마다 체크 후 다음 예정일 계산·삭제/되돌리기·매월 상시업무 기존 동작 유지·🔁 버튼이 실제로 사라졌는지까지 확인, 기존 회귀(`p3test`/`calroutinetest` 등) 전부 통과.
- **디자인 4차 — 배경·밀도·프로젝트 박스(2026-09-25)**: "배경이 순백이라 눈이 피곤하다 · 여백이 엑셀보다 널널하다 · 프로젝트가 사각형으로 안 감싸여서 구분이 잘 안 된다"는 피드백.
  - **라이트 테마 배경**: `--bg`를 순백(`#FFFFFF`)에서 채도 낮은 블루그레이(`#F1F3F6`)로 바꾸고 `--panel`은 흰색(`#FFFFFF`) 그대로 둠 — 그 결과 다시 `--bg`≠`--panel`이 돼서(디자인 2차 때 둘이 같아서 잃었던 배경/패널 구분이 부활) 흰 패널이 옅은 회색 캔버스 위에 도드라짐. `--sunk`/`--line`/`--line-2`도 새 배경에 맞춰 같이 재조정(다크 테마는 원래도 흰색이 아니었어서 안 건드림).
  - **여백 밀도**: `.wrap` 바깥 여백, `section.blk` 사이 간격, 표 행(`.tbl td/th`) 안쪽 패딩, 보드·저널·메모 리스트 패딩 등 주요 지점을 전반적으로 좁혀서 더 촘촘하게(표 폰트도 14→13.5px로 살짝 축소).
  - **프로젝트 박스 복원**: `.pjsec`(프로젝트 섹션)에 사각형 테두리(radius 없음)+흰 배경+박스 사이 여백을 다시 줘서 "한 프로젝트 = 한 박스"로 시각적으로 묶임(디자인 2차 때 걷어냈던 테두리를 프로젝트에 한해서만 되돌림 — 다른 섹션은 안 건드림).
- **캘린더 탭을 캘린더/일상 두 탭으로 분리(2026-09-25)**: "캘린더 탭과 일상 탭으로 분류하고 루틴/투두는 일상 탭에 넣어달라"는 요청. 캘린더 탭은 서브탭 없이 **달력 그리드만** 남기고, 루틴·투두 서브탭(`#calRoutinePanel`/`#calTodoPanel`)을 새 최상단 탭 **일상**(`data-tab="daily"`, `#dailySubtabs`)으로 옮김. 렌더 함수(`renderCal`/`renderCalRoutines`/`renderCalTodos`)는 손 안 대고 탭 구조·서브탭 이벤트 배선만 변경. 탭 순서 저장(`applyTabOrder`)에 "저장된 순서에 없는 새 탭이 섞여 있으면 저장된 순서를 버리고 기본 순서로" 조건을 추가(기존엔 반대 방향만 체크해서 새 탭 "일상"이 순서 맨 앞으로 튈 수 있었음).
- **루틴 트래커 — 깃허브 잔디st 히트맵(2026-09-25)**: "루틴탭에 트래커 보고 싶다"는 요청에 방식을 추천(잔디st 히트맵 vs 월 달력에 점찍기 — 여러 루틴을 한눈에 비교하기 좋은 잔디st 채택)하고 구현. `cal_routines.history`(jsonb, 체크한 날짜 배열, 마이그레이션 `cal_routines_history`, `hasRoutineHistory` 프로브) 추가 — 기존엔 "오늘 체크했는지"만 저장하고 지난 기록을 안 남겼어서, 트래커를 그리려면 체크할 때마다 날짜를 기록에 남기는 로직부터 새로 필요했음(**과거 기록은 없고 이 기능을 켠 시점부터 새로 쌓임**). `checkCalRoutine()`이 체크할 때마다 오늘 날짜를 `history`에 추가(매일 루틴은 토글이라 껐다 켰다 하면 오늘 날짜도 같이 추가·제거됨, 그 외 종류는 체크가 되돌릴 수 없는 동작이라 추가만 됨) — 최대 400개(약 1년치)까지만 유지. `#calRoutineTracker`(루틴 패널의 "예정된 루틴" 아래, 등록 폼 위)에 루틴마다 한 줄씩, 최근 30일을 가로 10px 칸으로 나열해서 체크한 날만 강조색으로 채움(`renderRoutineTracker`, `trackerDays`). 헤드리스 테스트(`trackertest.js`)로 체크/해제 시 오늘 칸 on/off, DB 저장, 요일 지정 루틴도 기록되는지까지 확인.
  - **버그 수정 — 체크해도 오늘 칸이 안 보이는 문제(2026-09-26)**: "체크 했는데 칸이 비어있어"라는 제보. 트래커는 왼쪽=옛날 → 오른쪽=오늘 순으로 30일(또는 그 달)을 나열하는 구조라 **오늘 칸이 항상 맨 오른쪽**인데, 그 줄이 화면(특히 모바일)보다 길면 `.trkgrid`의 `overflow-x:auto`가 기본적으로 맨 왼쪽(옛날)부터 보여줘서 방금 체크한 오늘 칸이 화면 밖으로 밀려나 있었음 — 체크 자체·저장은 멀쩡했고 단지 안 보였던 것. `scrollTrackersToToday(root)` 헬퍼를 만들어 트래커를 그릴 때마다 오늘 칸이 있는 오른쪽 끝으로 미리 스크롤해두게 함. **추가로 발견한 진짜 원인**: 이 스크롤 위치 지정을 탭이 아직 `display:none`(숨김)인 상태에서 하면 브라우저가 레이아웃이 없다는 이유로 값을 버리고 다시 0으로 리셋해버림(메모 탭의 `autoGrow` 찌그러짐 버그와 같은 종류의 문제) — 그래서 렌더 시점뿐 아니라 그 탭이 실제로 화면에 나타나는 시점(`switchTab`의 `daily`/`journal`, `dailySubtabs`의 `routine` 서브탭 전환)에도 다시 호출하도록 함. 헤드리스 테스트(`trackerscrolltest.js`)로 좁은 화면(390px)에서 오늘 체크 칸이 실제로 뷰포트 안에 들어오는지까지 확인.
- **저널 3분류(일상/업무/개인작업) + 루틴 트래커 통합(2026-09-26)**: "저널 회고에 일상/업무/개인작업 세 파트로 그날 완료된 투두가 나뉘어 보이면 좋겠다 · 루틴 트래커도 일별/월별로 볼 수 있게 저널에 넣어달라"는 요청.
  - **프로젝트 성격 지정**: `settings.projCategory`(`{프로젝트명: '일상'|'업무'|'개인작업'}`, 새 컬럼 없이 기존 `settings.data` jsonb 안에 얹음)로 프로젝트마다 성격을 저장. **홈 화면 프로젝트 헤더에 바로 드롭다운**(`.pjcat`, `renderProjects()`)으로 지정 — 이 셀렉트를 넣으려고 `.pjtog`를 `<button>`에서 `<div role="button">`으로 바꿈(select가 button 안에 있으면 브라우저마다 동작이 불안정해서). `projAll` 클릭 델리게이트가 이미 `[data-act]` 붙은 자식은 아코디언 토글에서 걸러내므로(진행기록·삭제 버튼과 같은 패턴) 이 select도 자연스럽게 안 걸림. **주머니(프로젝트 없는 할일)와 성격 미지정 프로젝트는 둘 다 "미분류"로 통일**(`projCategory()` 헬퍼가 빈 project나 미지정 시 자동으로 미분류 반환 — 별도 분기 코드 없이 자연스럽게 처리됨). 지울 때(미분류로 되돌릴 때) `delete`가 아니라 **명시적으로 `'미분류'` 문자열을 저장**하도록 함 — `putSettings()`의 병합(Object.assign 기반, `projOrder`처럼 여러 탭 동시 열림 대비용)은 "키를 지운다"를 표현 못 해서(로컬에서 지워도 서버에 남아있던 값이 되살아남), delete 대신 항상 값을 채워 넣는 방식으로 우회.
  - **저널 하루 칸**: 그날 ✓체크한 할일을 프로젝트 성격별로 묶어서(`groupByCat()`, 일상→업무→개인작업→미분류 순, 빈 그룹은 생략) 소제목(`.jcathead`)과 함께 보여줌. 그 아래 **"🔁 루틴" 섹션**을 추가해서 그날 체크한 캘린더 루틴(`dayRoutineChecks(d)`, `cal_routines.history`에 그 날짜가 있는지로 판별)도 같이 뜸. `checkCalRoutine()`이 루틴 체크할 때마다 `renderJournal()`도 같이 호출하도록 추가(안 그러면 루틴 체크 직후 저널 탭으로 바로 넘어가도 옛 상태가 보이는 버그가 있었음 — 다른 탭에서 체크하고 저널로 넘어가는 흐름을 헤드리스 테스트로 재현해서 발견).
  - **저널 월간 회고**: 완료 개수 요약 옆에 **카테고리별 개수**를 괄호로 덧붙임(예: "완료한 할일 5개 (일상 2 · 업무 3)"). 그 아래 **"이 달 루틴 트래커"** 섹션(`monthTrackerHTML(m)`)을 새로 추가 — 서랍의 최근 30일 롤링 잔디(트래커)와는 다르게, **그 달 1일~말일을 고정 컬럼**으로 써서 진짜 "이번 달 기준" 트래커를 보여줌(같은 `.trkwrap`/`.trkrow`/`.trkcell` CSS 재사용, 컬럼 개수만 그 달 일수만큼).
  - 헤드리스 테스트(`journalcattest.js`)로 프로젝트 카테고리 지정→DB 저장→저널 반영, 루틴 체크→저널 즉시 반영, 월간 회고 카테고리 요약·월간 트래커까지 확인. 기존 회귀 전부 통과.
- **꼭 이뤄야 할 것 — 업무/일상 2열(2026-09-26)**: 기존엔 "꼭 이뤄야 할 것" 3칸이 한 줄이었는데, 업무 3개·일상 3개로 나눠서 보고 싶다는 요청. `journal.musts` 배열을 3칸(index 0~2)에서 6칸(0~2=업무, 3~5=일상)으로 늘림 — 저장 코드(`mgSaveNow`)는 `[].map.call(document.querySelectorAll('.mgmust'),...)`로 **DOM 순서 그대로** 배열을 만들고, 읽기 코드(`renderMonthGoal`)는 `data-mi` 속성으로 인덱스를 매칭하므로, HTML을 업무 칼럼(data-mi 0~2)-일상 칼럼(data-mi 3~5) 순서로 배치하기만 하면 JS는 전혀 안 고쳐도 됨(`.mgmustcols` 안에 `.mgmustcol` 두 개, 680px 이하에선 세로로 쌓임). 기존에 이미 채워둔 값은 전부 "업무" 칼럼으로 그대로 보임(데이터 안 날아감).
- **꼭 이뤄야 할 것 → OKR 형식으로 변경(2026-09-26)**: "텍스트를 OKR로 바꿔달라"는 요청. 업무/일상 각 칸을 **목표(Objective) 1개 + 핵심 결과(Key Result) 3개**로 재구성 — `.mgmustcols` 안 각 `.mgmustcol`에 목표 입력(`.okrobj`, 굵게)을 맨 위에 추가하고 기존 3칸은 "핵심 결과"로 라벨만 바꿈(`.mgcolhead`도 "업무 OKR"/"일상 OKR"로), 칼럼 헤더 아래 목표 칸까지 합쳐 한 칸당 4개 입력창(목표+KR3개), 업무+일상 총 8개. **저장 로직은 안 건드림** — 이전과 똑같이 `mgSaveNow()`가 `.mgmust` 클래스 붙은 입력창을 DOM 순서대로 모아 `journal.musts` 배열에 저장하는 구조라, 목표 입력창도 `.mgin.mgmust.okrobj` 클래스를 그대로 받게만 해서(레이아웃만 4칸으로 늘어남) 코드 변경 없이 8칸 배열(0=업무 목표,1~3=업무 KR,4=일상 목표,5~7=일상 KR)로 자동 확장됨. **DB 마이그레이션 없음**(`journal.musts`가 이미 jsonb 가변배열이라 길이만 8로 늘어난 것). **예전 6칸(목표 없이 핵심결과만 있던) 데이터 호환**: `renderMonthGoal()`이 `musts.length<=6`이면 옛 형식으로 보고 옛 핵심결과 값들을 새 핵심결과 자리로 그대로 옮겨 보여줌(목표 칸은 비워서 새로 채우게 함, `OKR_LEGACY_MAP`) — 한 번이라도 저장하면 그 즉시 8칸 새 형식으로 넘어감. 헤드리스 테스트(`okrtest.js`)로 8칸 입력·저장·새로고침 복원·예전 6칸 데이터 승계까지 확인.
- **매일·주기·요일 상시업무가 프로젝트 박스에도 보이게(2026-09-26)**: "상시업무 중 매월이 아닌 건 서랍에서만 체크 가능한 것 같다, 전부 프로젝트에서 보여야 한다"는 지적. 원래 매일/며칠·몇주·몇달마다/특정요일마다 상시업무(`ROUTINE_TOGGLE` 종류)는 서랍의 `renderRoutines()`(`#routineAll`)에서만 체크할 수 있었음 — 홈 화면 프로젝트 박스(`renderProjects()`)엔 아예 안 보였음. `projectRoutineRows(name)`을 새로 추가해서, 대분류(project)가 지정된 상시업무를 그 프로젝트의 `.pjsec` 박스 맨 위(표 위)에 같은 체크리스트로 같이 보여줌(`routineCheckRow(r,true)` — 두 번째 인자로 프로젝트 배지를 숨김, 자기 프로젝트 박스 안이라 중복이라). **서랍에서도 그대로 유지**(양쪽에서 체크 가능, 같은 DB 행이라 어느 쪽에서 체크해도 서로 반영됨) — 서랍은 전체 등록·관리용, 프로젝트 박스는 그날그날 실제로 체크하는 자리로 역할을 나눈 셈. 프로젝트 미지정 상시업무는 원래대로 서랍에만 남음(어느 프로젝트에도 속할 데가 없으니까). `checkRoutine()`/`addRoutine()`/`rtc-del` 핸들러에 `renderProjects()` 호출을 추가해서 양쪽 화면이 항상 같이 갱신되게 함. 고치던 중 `toggle.map(routineCheckRow)`가 `Array.map`의 두 번째 인자(배열 인덱스)를 실수로 `hideProj`에 흘려보내던 버그(그래서 서랍 목록 둘째 항목부터 프로젝트 배지가 안 보였음)를 헤드리스 회귀(`routinemergetest.js`)로 잡아 `toggle.map(function(r){return routineCheckRow(r);})`로 고침.
- **콘텐츠 아카이브 탭(2026-09-26)**: "일정봇 아카이브를 하루앱에 통합, 가능하면 카카오/네이버 API로 자동 태그"를 "진행합니다"로 승인받아 구현.
  - **탭 신설**: `아카이브`(`data-tab="archive"`, 탭 순서 맨 끝). Supabase에 새 표 `archive`(id/owner/title/type/status/progress/rating/notes/tags/date_added/date_updated/date_completed/source/raw_log/author/year_watched/publisher/cover_url/sort — 봇의 `ContentEntry`와 같은 필드 구성, RLS `owner=auth.uid()`) 추가, `hasContentArchive` 프로브로 마이그레이션 안 된 DB에서도 안 깨지게 방어(변수명이 `hasArchive`가 아닌 건, `todos.archived`/완료함 기능이 이미 그 이름을 쓰고 있어서 — 일정봇 "아카이브"와 하루앱 기존 "완료함(archive)" 개념이 이름만 겹침, 서로 무관).
  - **UI**: 종류별 필터 칩(전체/책/웹툰/영화/…, 있는 종류만 개수와 함께 표시) + 목록(표지 썸네일·제목(클릭해서 바로 수정 가능)·종류·상태·평점) + 아래 빠른 추가 폼(제목+종류). 행의 ✎를 누르면 그 아래로 수정 박스가 펼쳐짐(캘린더 루틴의 주기 수정 박스와 같은 패턴) — 상태/평점/작가·감독/출판사/연도/링크/메모를 채우고 저장. 삭제는 되돌리기 토스트 지원.
  - **카카오/네이버 자동 채우기**: 수정 박스의 "🔍 자동 채우기"를 누르면 Supabase Edge Function `archive-search`(로그인 세션으로 인증, `verify_jwt:true`라 위젯 백엔드처럼 별도 토큰 안 만들어도 됨)를 호출 — 책이면 카카오 책 검색(설정돼 있으면 우선) 또는 네이버 책 검색, 영화/드라마/다큐/애니면 네이버 영화 검색으로 표지·저자(감독)·출판사·연도 후보를 몇 개 받아와서 목록으로 보여주고, 고르면 그 값들로 필드가 채워짐(표지는 `cover_url`에 저장돼 목록에 썸네일로 보임). 함수 자체는 이미 배포됨(project `mfgiesampazjzgfliuje`, 함수명 `archive-search`). **API 키는 아직 안 들어가 있어서 실제로는 "아직 설정 안 됨" 토스트만 뜸** — 카카오/네이버 개발자센터에서 앱 등록 후 받은 키를 Supabase 대시보드(Project Settings → Edge Functions → `archive-search` → Secrets)에 `KAKAO_REST_API_KEY` 또는 `NAVER_CLIENT_ID`+`NAVER_CLIENT_SECRET`로 넣어야 동작 시작(이 세션은 Edge Function 시크릿을 직접 설정할 방법이 없음 — MCP 도구에 그 기능이 없음). 왓챠·웹툰은 위 백로그 조사 결과대로 자동 검색 대상에서 제외.
  - **일정봇과의 연결**: 봇 섹션의 "아카이브 → 하루앱 거울복사" 참고 — 봇에서 새로 추가/수정하는 아카이브 항목이 이 표로 자동 복사됨. 기존 기록은 `/sync_archive_to_haru` 한 번 실행으로 옮김.
  - 헤드리스 테스트(`archtest.js`)로 추가/수정(평점·작가 저장)/삭제·되돌리기/필터 칩까지 확인, 자동 채우기는 스텁이 항상 "키 미설정" 응답을 주는 경로로 에러 없이 넘어가는지만 확인(실제 카카오/네이버 응답 파싱은 실기기에서 키 넣은 뒤 확인 필요). 기존 회귀 전부 통과.
- **모바일 UX 개선(2026-09-23)**: "가독성이 안 좋다 · 오른쪽 보드가 모바일에서 너무 많다"는 피드백으로 손봄.
  - **홈을 900px 이하에서 「할일 / 보드」 탭으로 분리**(`#homeSeg`, `#homeGrid.show-board`): 기본은 「할일」(오늘 띠+월간 표)만 보이고, 「보드」를 누르면 오늘한줄·주머니·회의안건·링크 네 개만 보임 — 예전엔 이 넷이 월간 표 밑에 그대로 이어 붙어서 스크롤이 두 배로 길었음. 「보드」 탭엔 **주머니 개수 + 안 끝난 회의 안건 수** 배지(`updateHomeSegCount`, `renderAll` 끝에서 매번 갱신)가 붙어서 안 눌러봐도 뭔가 있는지 알 수 있음. 주머니(`goPocket`) 클릭 시에도 모바일이면 자동으로 「보드」 탭으로 전환.
  - **버그 발견: 호버로만 보이던 버튼들이 터치에선 원천적으로 안 보임** — `.rowdel`(할일 삭제)·`.pjdel`/`.pjren`(프로젝트 삭제·이름변경)·`.subadd0`(＋하위 첫 추가)·`.subitem .sdel`(하위항목 삭제)·`.rowbtn`(🔁 반복설정, 오늘 토글)·`.nest`(⤵하위로)·`.sdue2`/`.stoday`(하위항목 날짜·오늘)·`.lkrow .lkdel`(링크 삭제) 전부 `opacity:0`+`:hover{opacity:1}` 패턴이라, 마우스 호버가 없는 폰에서는 **존재하는지도 모르고 있었거나 알아도 좌표 찍어 blind click** 해야 했음. 680px 이하에서 전부 `opacity:.5!important`로 항상 옅게 보이게 바꿈(`.has` 붙은 것들은 `.9`로 구분 유지, 이건 채워진 값이 있다는 뜻이라 특이도 계산상 더 구체적인 선택자로 따로 처리).
  - **마우스 전용 드래그 손잡이**(`.rowdrag`/`.pjhandle`/`.subdrag`, HTML5 Drag&Drop이라 터치에서 애초에 동작 안 함)는 680px 이하에서 **아예 숨김**(`display:none`) — 안 그래도 안 움직이는 걸 자리만 차지하고 있었음.
  - **제목 칸을 `td:has(>.ttl){display:flex;flex-wrap:wrap}` + `.ttl{flex:1 1 100%}`로 바꿈**: 제목이 이월·상시 배지, 🔁, ▸하위, 오늘, ⤵하위로, ✕ 같은 아이콘들과 한 줄 안에서 뒤엉켜 글자 단위로 쪼개지던 걸 고침 — 제목은 항상 자기 줄을 온전히 쓰고, 나머지 아이콘들은 그 아래 줄에 깔끔하게 줄바꿈됨.
  - **오늘의 집중·마감 임박 표에서 「프로젝트」 칸을 680px 이하에서 숨김**(`.blk table.tbl thead th:nth-child(3),.blk table.tbl td.pj`) — 월간 표(`.pjsec`)는 이미 프로젝트별로 묶여 있어서 이 칸이 원래 없음, 상단 두 표만 해당. 칸 하나 줄어든 만큼 제목 줄바꿈이 훨씬 덜해짐.
  - **"오늘 4시간 · 예상 …" 줄이 좁은 화면에서 글자 단위로 깨지던 것**: `.bhead`/`.bhead .r`가 `flex-wrap`이 없어서 한 줄에 욱여넣다 생긴 문제 → `flex-wrap:wrap`으로 그냥 다음 줄로 자연스럽게 내려가게 함.
- **탭 순서 변경**: 상단 탭 자체를 드래그로 재정렬(localStorage `tabOrder`, `applyTabOrder`).
- **모바일 전체화면(아이폰 메모st)**: 680px 이하에서 그림공부/메모 둘 다 "목록 vs 상세" 중 하나만 보임 — 뭔가 선택하면(작가/그림/태그/미분류, 또는 메모) 사이드바·목록이 숨고 내용이 화면 전체를 채우는 고정 오버레이로 뜸(`art-mob-detail`/`mob-edit` 클래스, `renderArtMain`/`#mBackBtn`에서 토글). 각 화면 위 **‹ 목록** 링크(`art-back-list`)나 **‹ 메모 목록** 버튼(`#mBackBtn`)으로 되돌아감. 데스크톱(680px 초과)에선 원래대로 사이드바+본문 동시에 보임(CSS 미디어쿼리 안에서만 의미있는 클래스라 무해).
- **되돌리기**: 토스트 "되돌리기" + **Cmd/Ctrl+Z**(입력창 focus 아닐 때). `undoHist` 스택, `doUndo()`. (되돌리기 옵션 있는 액션만 — 삭제/완료함/nest/프로젝트삭제 등)
- **프로젝트 이름 변경**: 헤더 hover **✎**(`proj-rename`) → 해당 ws 모든 할일 `project` 갱신 + projOrder 키 이동.
- **메모 정렬**: 최신순(`memos.updated` text, `putMemo`가 `new Date().toISOString()` 기록). 새 메모/수정 메모가 위로(목록 새로고침 시). 컬럼 없으면 `hasMemoUpd`로 방어.
- **전체 검색**: 헤더 아래 검색바(`#q`) — 모든 탭/워크스페이스 걸쳐 할일(제목·하위항목·프로젝트)+메모 검색. **띄어쓰기 무시**(`norm()`=공백 제거+소문자). 결과 클릭 시 해당 위치로 이동. (`runSearch`, `#searchView`)
  - **대시보드**: 빠른 담기 + 오늘의 집중(자동 우선순위 + 가용시간 · **오늘 찍음 or 오늘 마감 자동 포함**, 밀린 건 마감 섹션에만) + 마감 임박. 진짜 할일만. 빠른담기 옆 **오늘** 버튼=오늘의 집중 바로 담기(Shift+Enter도). 행 hover **🔁**=고정업무(none→매일→매주(요일)→매월(일)→none), 프로젝트 행 hover **오늘** 토글, 하위 항목 hover **＋날짜**(`subdue`, sub.due) · **오늘**(`subtoday`, sub.today — 하위 항목도 오늘의 집중에 개별 지정 가능, 마감 안 잡아도 됨). **오늘 지정은 자정 지나면 자동 리셋**(`dailyResetToday`, 매일 첫 로드 때 `settings.todayResetDate`가 오늘과 다르면 부모·하위 `today` 플래그 전부 끄고 오늘 날짜로 갱신 — 컬럼 추가 없이 settings에 리셋한 날짜만 기록해서 판별). **오늘의 집중 포함 조건**: 부모가 `today`거나 마감이 오늘이거나, **하위 항목 중 하나라도 `today`거나 마감이 오늘**이면(완료 제외) 그 부모가 통째로 올라옴. **마감 임박·밀린 것에 하위 항목 마감도 포함**(`renderDue`) — 예전엔 상위 할일의 `due`만 봐서 하위 항목에만 마감을 걸면 그날이 아니면 어디서도 안 보이던 버그 → 상위+하위 마감을 합쳐서 최대 12개, 가까운 순으로 표시(하위 항목은 `↳ 상위 제목`으로 구분, 체크박스는 `subck` 재사용). **고정업무 완료 이력**(`todos.donelog jsonb`, `hasDoneLog` 프로브): 체크할 때마다(같은 날 중복 방지) 날짜를 누적 기록 — 예전엔 `lastdone` 최신 날짜 하나만 남아서 "이번 주에 며칠 했는지" 확인이 아예 불가능했음. 지금은 반복 배지 옆에 **N회** 로 누적 완료 횟수 표시(`repDoneCount`).
  - **주머니·프로젝트**: 주머니 분류(오늘/프로젝트/나중/메모/버림) + 프로젝트 아코디언. **주머니 표시 기준은 `bucket==='inbox'`가 아니라 `!project`**(프로젝트가 없으면 무조건 주머니) — "오늘" 버튼으로 바로 오늘의 집중에 던진 항목(`bucket='active'`, project 없음)이 다음날 `dailyResetToday`로 오늘 지정이 풀리면 프로젝트도 없고 bucket도 inbox가 아니라서 주머니/프로젝트 어디에도 안 보이고 마감임박에만 뜨는 유령 상태가 되던 버그 → 모든 할일이 항상 주머니 또는 프로젝트 중 하나에는 걸리게 통일. 주머니 행에도 체크박스(완료) 추가. **주머니 행에 마감일 지정 버튼**(`📅 날짜`, `inbdue`, `due` act 재사용)도 추가 — 프로젝트 없이 주머니에만 있는 항목도 마감을 오늘로 잡으면 오늘의 집중에 바로 뜸(대시보드 필터가 이미 `daysTo(t.due)===0`을 bucket 상관없이 보고 있어서 버튼만 추가하면 됨). **주머니에서 체크하면 완료함으로 버튼**: 예전엔 주머니 행을 체크(done)해도 완료함으로 보낼 방법이 없어서(프로젝트/오늘의 집중 뷰에만 그 버튼이 있었음) 체크한 항목이 주머니에 done 상태로 계속 남아있었음 → 주머니 행도 `t.done`이면 다른 액션 버튼들 앞에 `완료함으로` 버튼이 뜨게 추가(`renderInbox`, 기존 `archive` 액션·`putTodo` 그대로 재사용). **프로젝트 안에서 바로 할일 추가**: 예전엔 새 할일이 항상 주머니로 먼저 들어가고 나서 그 프로젝트로 옮겨야 했음(`t-proj` select) → 각 프로젝트 섹션(`.pjbody`) 표 아래에 입력창(`.pjaddin`+`추가` 버튼, `.pjadd`)을 추가해서 `addProjectTodo(proj,title)`로 그 프로젝트에 바로(`bucket:'active'`, 주머니 안 거침) 담을 수 있음 — Enter 또는 버튼 클릭, `data-act="proj-add"`.
  - **캘린더**: 월 그리드, 드래그로 마감일 변경. **기간 일정**(`todos.endd`)이면 시작~종료 모든 날에 바 표시(시작일=제목, 이후=`.evcont` 연속 바). 구캘 일정이 여기 뜸.
  - **메모**: 애플 메모st, 폴더, 할일 연결(📝). **라이브 블록 에디터**(`#mbodyTree`) — 예전엔 편집(raw textarea) ↔ 읽기(렌더링) 뷰를 탭해서 전환했는데(리치 에디터로 교체하면서 폐기), 지금은 **읽기/편집 구분 없이 하나의 화면**에서 불릿·체크박스·토글·사진이 실시간으로 그 모양 그대로 보이고 바로 고칠 수 있음(노션st). 저장 형식(마커 붙은 순수 텍스트, `m.body`)은 그대로라 옵시디언 연동 안 하기로 한 결정과도 무관하고 마이그레이션도 필요 없음.
    - **세그먼트 트리 구조**: 본문을 파싱해서(`buildSegmentsInto`/`buildSegmentsIntoTagged`) 줄 단위 `<textarea class="mseg">`(연속된 일반/불릿/체크박스 줄 묶음) + 토글(제목 `<textarea class="mseg-header">` + 접었다 펼 수 있는 `.mtoggle-body` 안에 재귀적으로 같은 구조) + 사진(`.mreadimg-wrap`, 실제 `<img>`) 세그먼트로 이루어진 DOM 트리를 만듦(`renderMemoTree`). 각 세그먼트는 자기 컨테이너/부모리스트에 대한 역참조를 들고 있어서(`attachSeg`, `segFor`) 어떤 textarea에서 이벤트가 나든 그 세그먼트를 바로 찾음. 전부 `#mbodyTree`에 이벤트 위임(input/keydown/click/paste/drag), `focusout`으로 멘션 팝업만 닫음(blur는 버블 안 해서 focusout 사용).
    - **저장(직렬화)**: `syncBodyFromTree`/`serializeSegs`가 트리를 걸어다니며 각 세그먼트의 **살아있는 textarea 값**을 그대로 읽어서 `m.body` 문자열로 합침 — 들여쓰기는 세그먼트 자체엔 안 남기고(자식 textarea엔 순수 텍스트만 보임) **직렬화할 때 트리 깊이로만 재계산**(`depth*2`칸). 그래서 접힌 토글 안 내용을 편집해도(`.mtoggle-body.hidden`이어도 DOM엔 그대로 있음) 저장 시 안 새고, 다시 열어도 정확히 같은 깊이로 복원됨.
    - **접기 토글, 이제 편집 중에도 실시간으로 접힘**(원래 있던 요청): `>`+스페이스 치면 그 텍스트 세그먼트를 앞/토글/뒤 세그먼트로 실제로 쪼갬(`splitTextSegIntoToggle`) — 이제 토글은 진짜 별도 DOM(제목 textarea + 접었다 펼 수 있는 바디 div)이라, 화살표 클릭하면 `.mtoggle-body`에 `hidden` 클래스만 토글— **접혀도 안에 든 textarea들은 그대로 살아있어서 데이터 유실 없음**(숨김 ≠ 삭제, 헤드리스 테스트로 접기 전후 자식 값이 100% 동일함을 확인함). 제목에서 엔터 → 그 아래 자식 세그먼트 새로 시작 + 자동 펼침, **제목이 비어있고 자식도 없을 때 엔터 → 토글을 통째로 빈 줄 세그먼트로 되돌림**. 중첩 토글(토글 안에 토글)도 재귀 구조라 지원.
    - **여러 줄 붙여넣기**: 들여쓰기 있는 줄에 붙여넣으면 첫 줄 제외 나머지 줄에도 커서 줄의 들여쓰기를 그대로 붙여서 삽입(세그먼트가 이제 분리돼 있어서 예전처럼 "토글 밖으로 새는" 문제 자체가 구조적으로 없어짐, 이 로직은 세그먼트 안에서 수동 들여쓰기하는 경우 대비 유지). 붙여넣은 텍스트에 `▾`/`▸`나 사진 마커가 섞여있으면(다른 메모에서 복사해온 경우 등) **줄 경계를 확보한 뒤 전체 트리를 재구성**(`renderMemoTree`)해서 진짜 토글/사진으로 살아남. 출처별 줄바꿈 문자(`\r\n`/`\r`/유니코드 줄바꿈)도 다 `\n`으로 정규화.
    - **체크리스트**: `[]`/`[ ]`+스페이스 → `☐ `, 엔터로 다음 줄도 `☐ ` 이어짐(항상 미체크로 시작), 빈 체크박스에서 엔터 → 종료. **글리프(☐/☑) 탭/클릭으로 완료 토글**(세그먼트 textarea 클릭 위치가 글리프 근처(±2자)일 때만 토글).
    - **탭으로 들여쓰기**: 불릿/체크박스/일반 줄 어디서든 `Tab`으로 현재 줄 앞에 2칸 들여쓰기 추가(하위 항목처럼 보이게), `Shift+Tab`으로 내어쓰기(있으면 최대 2칸까지 제거). 세그먼트 안의 순수 텍스트(공백)라 렌더링/저장에 별도 처리 필요 없음 — 토글처럼 별도 구조가 되는 게 아니라 그냥 그 텍스트 세그먼트 안에서 시각적으로만 들여써짐.
    - **실행취소**: 세그먼트가 다 별도 textarea라 `.value`를 직접 건드리는 순간 브라우저 기본 undo가 깨져서, **자체 되돌리기 스택**을 만듦(`memoUndoStack`, 메모 하나당 최대 50단계, 메모 바꾸면 `resetMemoUndo`로 리셋). 타이핑처럼 연속으로 이어지는 입력은 디바운스 저장(600ms)이 끝날 때까지 한 묶음으로 스냅샷 1개만 찍고(`checkpointMemoUndo`/`memoUndoBurstOpen`), 토글 생성·사진 추가/삭제·멘션 연결처럼 뚜렷이 구분되는 동작은 그 자체로 바로 스냅샷 하나씩(즉시 저장 + burst 강제 종료) — 그래서 "사진 붙였다 바로 지우기"처럼 빠르게 이어져도 되돌리기 단계가 안 섞임. 스냅샷은 `m.body`뿐 아니라 **`m.images`도 같이 저장**(안 그러면 사진 삭제를 되돌렸을 때 마커 줄만 돌아오고 정작 사진 URL은 안 돌아옴). PC는 `Cmd/Ctrl+Z`(세그먼트에 포커스 있을 때, 브라우저 기본 되돌리기 대신 가로챔), 모바일은 편집기 상단 **↺ 실행취소 버튼**(스택 비어있으면 비활성화) — 어느 쪽이든 `doMemoUndo()` 하나로 처리.
    - **링크 탭하면 열림**: 문장 중간의 `https://…`/`www.…` 텍스트를 탭/클릭하면 새 탭으로 열림(`MEMO_URL_RE`, 체크박스 글리프 탭과 같은 클릭 핸들러 — 클릭 위치가 URL 문자 범위 안일 때만 동작, 그 외엔 그냥 커서 이동). 순수 textarea라 **색이나 밑줄로 꾸며지진 않음**(문장 일부만 다르게 스타일하는 게 구조적으로 불가능 — 토글 안에 있는 것과 같은 제약) — 겉보기엔 평범한 텍스트지만 탭하면 열림. `www.`로 시작하면 `https://`를 붙여서 엶.
    - **사진 첨부**(`memos.images` jsonb, `hasMemoImg` 프로브): 제목 아래 **📎 사진** 버튼, 본문 아무 세그먼트에 붙여넣기(Cmd/Ctrl+V)·드래그앤드롭 다 가능(그림공부와 같은 `uploadArtImage`/`art` 스토리지 버킷 재사용). 커서가 있던 텍스트 세그먼트를 그 자리에서 앞/사진(들)/뒤로 쪼개서 삽입(`addMemoImages`) — **사진은 항상 실제 `<img>`로 보임**(예전처럼 편집모드에선 마커 텍스트로만 보이던 문제 없음), 탭하면 새 탭으로 보기·✕로 삭제(`deleteImgSegment`). 삭제 시 인덱스가 밀리는 문제를 피하려고 트리 전체를 다시 그림. **사진 붙인 뒤 포커스를 항상 사진 바로 뒤 빈 텍스트 세그먼트로 이동**시킴(안 그러면 포커스가 아예 날아가서, 연달아 사진 두 장을 붙였을 때 두 번째 사진이 첫 번째 사진 앞으로 끼어드는 순서 버그가 있었음). **사진 위치 이동**: 사진 위 ▲▼ 버튼으로 한 줄씩 위/아래 이동(모바일에서 정확하게), 또는 사진 자체를 드래그해서 다른 세그먼트 앞에 놓기(`moveImgLine`/`moveImgSegBefore`) — 마커끼리 자리를 바꾸면 `m.images` 배열도 문서 순서에 맞게 같이 재정렬(`collectImgSegs`로 전체 사진 순서를 다시 훑어서 배정, 마커 순서와 배열 인덱스가 항상 1:1 유지되게).
    - **빈 공간 클릭 → 문서 끝으로 커서(2026-09-23)**: 본문 영역(`.mtree`)은 `min-height:320px`로 늘 크게 보이는데, 실제 커서가 들어가는 곳은 글자가 있는 그 줄(빈 메모면 한 줄, `.mseg{min-height:1.5em}`)뿐이었음 — 그 아래 빈 공간을 눌러도 아무 반응이 없어서 "쓰는 자리가 너무 작다"는 피드백을 받음. `#mbodyTree`의 `click` 핸들러에서 **`e.target`이 컨테이너 자체(=자식 세그먼트가 아닌 빈 공간)일 때** 마지막 `textarea.mseg:not(.mseg-header)`에 포커스 + 커서를 그 끝으로 보냄(노트 앱들의 "빈 곳 탭하면 맨 끝으로" 동작과 동일).
    - **목록 맨 아래쪽 메모 클릭 → 본문이 화면 밖에 있던 버그(2026-09-23)**: 목록(`#mlist`)을 스크롤해서 맨 아래쪽 메모를 클릭하면, 페이지 스크롤 위치는 그대로인데 방금 연 메모의 본문(`.medit`)이 짧으면 화면 위쪽(스크롤한 지점보다 위)에 있어 안 보이고 다시 위로 스크롤해야 하는 문제. `#mlist`의 클릭 핸들러에서 메모를 연 직후 `document.querySelector('.medit').scrollIntoView({block:'start'})`를 호출해서 본문이 항상 화면에 바로 보이게 함.
    - **메모 탭 처음 들어갈 때만 본문이 찌그러져 보이던 버그(2026-09-23)**: "메모 탭에 처음 들어가면 내용이 안 보였다가, 다른 메모 들어갔다 다시 오면 보인다"는 제보. 원인은 `autoGrow(ta){ta.style.height='auto';ta.style.height=ta.scrollHeight+'px'}` — 앱이 켜질 때(`renderAll`) 메모 탭이 아직 `display:none`(`.hidden`)인 상태에서 `renderMemoTree`가 한 번 먼저 그려지는데, **숨겨진 요소의 `scrollHeight`는 0**이라 그 시점에 계산된 높이가 거의 0으로 굳어버림(`.mtree{min-height:320px}` 덕분에 컨테이너 자체는 커 보여도 안의 `textarea.mseg` 하나하나는 찌그러진 채). 탭을 눌러 보이게 만들어도 이미 인라인으로 박힌 `height`는 저절로 안 바뀌고, **다른 메모를 거쳐야**(그 메모를 열 때 다시 `renderMemoTree`가 돌면서, 이번엔 탭이 보이는 상태라 정상 계산) 고쳐졌던 것. `switchTab('memo')`로 들어올 때마다 `#mbodyTree`의 기존 `textarea.mseg` 전체에 `autoGrow`를 다시 돌려서 고침. 헤드리스 테스트로 재현(수정 전: 높이 21px로 찌그러짐 / 수정 후: 실제 208px)해서 진짜 원인이 맞는지 확인 후 픽스.
    - **새 메모 만들면 바로 편집 가능한 상태**로 열림(제목부터 바로 쓸 수 있게, 읽기/편집 전환이 없어져서 별도 모드 전환 자체가 불필요해짐).
    - **삭제**: 편집기 상단 **🗑 삭제** 버튼(`#mDelBtn`) — 되돌리기 토스트 지원, 연결된 할일 있으면 memoId도 같이 정리(되돌리면 복원).
  - **완료함**: 체크=완료(찍 긋고 그 자리 유지 + 완료시점 `doneat` 기록) → "완료함으로" 버튼/일괄정리로 `archived=true` → 완료함 탭에서 완료시점 표시 + 되돌리기. 반복 항목은 예외(lastdone).
  - **프로젝트**: 헤더 ⠿ 핸들 드래그로 순서 변경(→ `settings.projOrder[ws]`), ✕로 삭제(할일은 주머니로 이동, 삭제 아님). 프로젝트 안 **할일 행 ⠿ 드래그로 순서 변경**(→ `todos.sort`, `reorderTodo`), **하위 항목 ⠿ 드래그로 순서 변경**(subs 배열 재정렬, `reorderSub`).
  - **⤵하위로(nest) 연결 모드 주의**: 모드 진입 후 아무 데나 클릭하면 취소됨(예전엔 유효 대상 아니면 무시→전체 먹통 버그였음, 수정됨), Esc로도 취소.
  - **하위 항목(서브태스크)**: 할일 제목 옆 `▸ n/m` 칩(또는 hover 시 `＋하위`) → 토글로 체크리스트 펼침. 오늘의 집중·프로젝트 뷰에서만. `todos.subs jsonb`(`[{t,d}]`)에 저장. 기존 할일은 hover `⤵하위로` → 대상 할일 탭하면 그 밑으로 이동(원본 삭제, 자식의 subs도 함께 흡수, 되돌리기 지원).
  - **DB 추가**: `todos.doneat text`, `todos.archived bool`, `todos.subs jsonb`(각 sub `{t,d,id,due}`), `todos.endd text`(기간 일정 종료일), `todos.donelog jsonb`(고정업무 완료 날짜 이력), `settings(owner uuid pk, data jsonb)` 테이블(RLS `owner=auth.uid()`). 코드는 `hasArchive`/`hasSubs`/`hasEnd`/`hasDoneLog` 플래그로 컬럼 없어도 안 깨지게 방어.
  - **`putSettings()` 여러 탭 동시 열림 대비**: `/cha`(찻잎 계산기)에서 탭 여러 개 열어두면 오래된 탭이 최신 저장을 낡은 값으로 덮어쓰는 버그가 있었는데, 하루앱의 `settings`도 사용자당 통째로 한 행(JSON 하나)이라 똑같은 구조적 위험이 있음(`todos`/`memos`/그림공부는 각 행을 id로 upsert해서 원래부터 안전, `settings`만 위험). `putSettings()`가 이제 쓰기 직전에 서버 최신값을 다시 읽어와 내 변경사항과 합친 뒤 저장함(`projOrder`는 워크스페이스별로 한 번 더 합쳐서, 내가 모르는 다른 워크스페이스 순서도 안 날아가게 함) — 프로젝트 순서 변경·자정 리셋 날짜 기록에 적용됨.
- **우선순위 점수**: `중요도*25 + 마감임박도(지남100/오늘·내일60/3일내30) + 빠른완수(≤20분)10`
- **Supabase**
  - Project URL: `https://mfgiesampazjzgfliuje.supabase.co`
  - Publishable key(공개용, 앱에 하드코딩 OK): `sb_publishable_QBlLIrJ8coqH3I-Ij-Q7SA_OUB7KVwV`
  - ⚠️ secret key는 절대 코드/문서에 넣지 말 것
  - 테이블: `todos`, `memos`, `settings`, `art_artists`, `art_works` (RLS `owner=auth.uid()`) + **v2 추가**: `progress`(진행 기록, `todo_id`·`date`·`text` — 덮어쓰지 않고 쌓임) · `routines`(상시업무 — 매월/격월/매일/주기/요일 5종, 아래 참고) · `journal`(`date`가 `YYYY-MM`이면 그 달의 월간 회고, `goal`/`musts`엔 월 목표·꼭 이뤄야 할 것) · `links`(`pinned`면 홈 오른쪽 고정) · `agenda`(회의 안건) · `push_subscriptions`(웹 푸시 구독, `endpoint` PK) · `cal_routines`(캘린더 탭 루틴, 일상용 자유 주기 체크리스트 — 업무용 `routines`와는 완전히 별개, 안 섞음) · `cal_todos`(캘린더 탭 투두, 캘린더에 안 뜨는 단순 체크리스트) · `archive`(콘텐츠 아카이브 — 책/웹툰/영화 등, 일정봇 Archive 시트를 거울복사, `hasContentArchive` 프로브). `todos`엔 `month`/`review`/`routine_id`/`rolled` 컬럼, `memos`엔 `pinned`(홈 오른쪽 고정, 2026-09-23), `journal`엔 `goal`(text)/`musts`(jsonb, 2026-09-23), `cal_routines`엔 `slot`(text, 2026-09-25 — 매일 루틴 아침/점심/저녁 구분)·`history`(jsonb, 2026-09-25 — 루틴 트래커용 체크 날짜 기록, 마이그레이션 `cal_routines_history`, `hasRoutineHistory` 프로브), `routines`엔 `unit`/`n`/`weekdays`(jsonb)/`next_due`/`done_today`(2026-09-25, 마이그레이션 `routines_toggle_kinds` — 고정업무 통합, `hasRoutineToggle` 프로브) 컬럼 추가. `settings.data`(jsonb) 안에는 새 컬럼 추가 없이 `projCategory`(2026-09-26 — 프로젝트별 일상/업무/개인작업 성격) 키만 얹음. 전부 추가만 한 마이그레이션이라 기존 행은 그대로.
  - **프로브 플래그**: 새 컬럼·테이블도 기존 방식대로 `hasMonth`/`hasAgenda`/`hasLinks`/`hasProgress`/`hasJournal`/`hasRoutines`로 확인 후 켜짐 — 마이그레이션 안 된 DB에서도 앱이 안 깨짐.
  - **헤드리스 테스트 하네스**(`scratchpad/mkstub.js`): 샌드박스에선 supabase.co·cdn.jsdelivr.net이 **에그레스 프록시에 막혀 있어** 실제 앱을 못 띄움 → `index.html` 복사본의 supabase CDN 스크립트를 **가짜 supabase 스텁**(체이너블 `from().select().eq().upsert()` + `auth`)으로 바꿔치기해서 Playwright로 돌림. 가짜 DB는 `sessionStorage`에 저장돼 **새로고침해도 유지됨** — "달이 바뀐 뒤 다시 열기"(이월) 같은 시나리오를 재현하려면 이게 필요함(안 그러면 reload마다 DB가 초기화돼서 테스트가 헛돎). 배포 전엔 이걸로 전 기능 회귀를 돌릴 것.
    - ⚠️ **이 스텁은 가짜라 실제 Postgres 제약(컬럼 타입·RLS·overflow)을 안 걸러냄** — v2 3단계 전체가 이 스텁으로만 검증되고 배포됐다가, 아래 `sort` 정수 오버플로 버그를 못 잡고 그대로 나간 전례가 있음. **진짜 저장 여부는 배포 후 `mcp__Supabase__execute_sql`로 실제 DB 행 수를 직접 세어서 확인할 것** — 스텁 테스트 통과를 저장 성공의 증거로 여기지 말 것.
  - **버그: `sort` 정수 오버플로로 회의 안건·상시업무·링크가 조용히 저장 안 됨(2026-09-23 발견·수정)**: `agenda`/`routines`/`links` 세 표의 `sort` 칸이 `integer`(최대 약 21억)인데, 새 항목 추가 코드(`addRoutine`/`agendaIn` Enter/`addLink`)가 순서값으로 **`Date.now()`(밀리초, 13자리 ≈ 1.7조)를 넣어서 범위 초과 → INSERT가 서버에서 거부**됨. 화면엔 로컬 상태로 즉시 보여서 저장된 것처럼 보이지만, `putAgenda`/`putRoutine`/`putLink`가 에러를 `console.warn`으로만 삼켜서 **사용자에게 실패가 전혀 안 보이고, 새로고침하면 사라짐**. 실제 DB에 같은 값으로 직접 `insert`해서 `22003: integer out of range`로 재현 확인 후, 세 칸을 `bigint`로 `alter column`(앱 코드는 안 건드림 — `Date.now()`를 그대로 써도 안전해짐). **진행 기록(`progress`)·저널(`journal`)은 `sort` 칸 자체가 없어서 무관**, 기존 할일·메모·프로젝트 추가는 `sort:0`이나 드래그 순서 정수를 써서 애초에 무관 — 코드 전체를 `Date.now()`로 grep해서 이 셋 말고 다른 숫자 컬럼에 쓰이는 곳 없음을 확인함.
  - **백업**: 큰 구조 변경 전엔 전체 테이블을 JSON 한 벌로 떠서 사용자에게 파일로 보낼 것(컨테이너는 세션 끝나면 사라짐). 2026-09-22 백업 시 메모 본문에 **U+00A0(줄바꿈 없는 공백)·U+2028(줄 구분자)** 가 섞여 있는 걸 발견 — 메모 직렬화·비교 코드에서 이걸 일반 공백으로 바꾸면 줄이 깨지니 주의.
  - Auth: email+password (Confirm email 꺼둠). **자동 로그인 없음** — 예전엔 `localStorage.haru_creds`에 이메일/비번을 평문으로 저장해두고 앱 열 때 자동 로그인했는데, 링크만 열면 바로 내 할일이 보이는 게 싫어서 제거함(회사에 `/cha` 링크를 공유하게 되면서). 남아있던 `haru_creds` 값도 로드 시 지움. 아이디/비번 기억은 **브라우저 자체 비밀번호 관리자**에 맡김 — 그래서 로그인 입력칸을 `<form id="authForm">`(+`name`/`autocomplete=username·current-password`, 로그인 버튼 `type=submit`)으로 감쌈. 한 번 로그인하면 Supabase 세션(`persistSession`, `haru_session`)은 그대로 유지돼서 매번 비번을 치진 않음.
- **호스팅**: Cloudflare (`haru.lolcv1294.workers.dev`). 자동배포는 이 repo `main` 브랜치 연결 (아래 배포 메모 참고).
- **`/cha` — 한강점 찻잎 도매 장부**(`webapp/cha.html`, 별도 정적 페이지): 하루앱과 완전히 분리된 단일 HTML. **로그인 없이 누구나 열림**, 데이터는 Supabase `cha_store` 표(공개 read/write, `key`+`value jsonb`)에 저장돼 **어느 컴퓨터·폰에서 열어도 같은 최신 데이터**를 봄(2026-09-07 이전) — 화면 폭은 `.wrap{max-width:1400px}` — 원래 780px이었는데 한 번 여백을 완전히 없애봤다가(`max-width:none`) 넓은 화면에서 내용이 헐렁하게 늘어져 보여서 780px로 되돌렸고, 그 다음 780↔없음의 중간인 1000px → 1400px로 계속 조정함(모바일·탭 화면 폭엔 영향 없음, 이 값만 계속 바뀌어옴). **최상위 탭은 장부 / 차 백과 2개**(`#mainTabs`)뿐이고, 그 안에 **내부 탭**(`.tabs.subtabs`)이 또 있는 2단 구조: 장부(`#ledgerTabs`) = **한강점**(운영·발주 설정 + 6종 블렌딩 구성/원가·마진 + 차종별 최적 우림법 + 발주 요약) → **구매처**(차 종류별 구매처 비교표 + **구매처 연락처**: 구매처·담당자·전화번호·비고를 편집/추가/삭제 가능한 표, `vendorContacts`, `jamwon-tea-vendor-contacts-v1`) → **다과**(다과 단가표 + 다과 상 구성) 순서. 차 백과(`#guideTabs`) = **백과**(중국·대만·일본·한국·동남아 품종표 + 발효도 스펙트럼) → **우림가이드**(차 30종 우림 스펙 + 계산기) → **향미사전**(향 계열·다도 용어·봉황단총 향형) 순서. 탭 전환은 `wireTabGroup(barId, storageKey)`로 그룹별 스코프를 잡아서(`:scope > .tab`) 서로 안 섞이고, 각 그룹의 마지막 선택 탭을 그룹별 `localStorage` 키로 따로 기억함(`restoreActiveTab`). **내부 탭도 최상위 탭 바로 밑에 같이 고정(sticky)됨** — 최상위 탭 바 높이를 `syncSubtabsStickyOffset`이 실측해서 CSS 변수(`--subtabs-top`)로 넘겨주는 식이라, 폰트 로딩이나 화면 폭이 달라져 최상위 탭 높이가 바뀌어도 내부 탭이 그 바로 밑에 딱 붙음(리사이즈·로드 시 재계산). **구매처 비교표의 "구매처" 칸은 `<input list>` + `<datalist id="vendorNameList">`로 구매처 연락처의 이름 목록을 드롭다운 자동완성으로 제공**(`renderVendorNameDatalist`, 구매처 연락처가 바뀔 때마다 갱신) — 그림공부 작가 선택과 같은 패턴, 목록에 없는 이름도 자유 입력 가능. 원래 클로드 아티팩트로 만든 걸 옮긴 거라 **`window.storage`(아티팩트 전용 API)를 셰임으로 교체**해야 저장이 동작함 — 나중에 아티팩트에서 새 버전을 가져올 때도 이 부분을 꼭 다시 갈아끼울 것. **저장소를 localStorage → Supabase로 이전(2026-09-07)**: 처음엔 로그인 없이 그 브라우저 `localStorage`에만 저장했는데, 여러 컴퓨터에서 쓰다 보니 기기마다 데이터가 따로 저장돼 "새벽에 입력한 게 다른 컴퓨터에서 안 보인다"(사실은 안 사라진 것 —애초에 그 기기엔 저장된 적 없는 것뿐이었음, localStorage는 기기별로 완전히 독립적이라 서로 덮어쓸 방법 자체가 없음)는 혼란이 생김 → `window.storage.get/set`을 Supabase `cha_store` 표 기반으로 교체(로그인 없이 계속 공개 read/write, RLS `using(true)`). 서버에 그 키 값이 아직 없으면 그 기기의 localStorage 값으로 1회성 이전(seed) — 그래서 이 변경을 배포한 뒤 **가장 최신 데이터를 가진 기기(예: 집 컴퓨터)에서 제일 먼저 열어야** 그 값이 서버의 기준값이 됨(다른 기기를 먼저 열면 그 기기의 옛날 값이 먼저 서버를 채워버림). localStorage는 오프라인/네트워크 실패 시 마지막 값을 보여주는 캐시로만 계속 사용(진짜 원본은 항상 서버). 표 칸 너비(`jamwon-tea-colw-*`)는 화면마다 다른 게 자연스러운 순수 UI 취향이라 그대로 기기별 localStorage에만 남겨둠(동기화 안 함). **구매처 비교표 항목은 안 바뀌는 `id`로 블렌딩 구성표와 연결**(`rowKey`) — 예전엔 "이름__구매처" 문자열로 연결해서 비교표에서 이름만 고쳐도 블렌딩표 선택이 조용히 "직접 입력·0원"으로 끊기는 버그가 있었음, 지금은 이름을 고쳐도 안 끊김(예전 데이터도 열 때 자동 이전). **표/백과 검색은 엑셀 찾기st**(`makeFinder`) — 행을 숨기지 않고 n/m 카운터로 순서대로 이동, input 값은 `rowInputText`로 따로 모아서 검색(`textContent`만 쓰면 `<select>`의 선택 안 된 옵션까지 걸려서 아무 분류명이나 검색해도 전체 행이 잡히는 버그가 있었음). 검색엔진 노출은 `<meta name="robots" content="noindex">`로 막아둠(단가가 공개돼 있어서). ⚠️ URL을 아는 사람은 누구나 도매 단가를 볼 수 있음. **회사 공유용이라 하루앱으로 돌아가는 링크는 일부러 안 넣음** — 이 페이지에 하루앱 흔적을 남기지 말 것. **표 칸 너비 드래그 조절**(`setupColResize`): 구매처 비교표·구매처 연락처·다과 단가·블렌딩 구성표·최적 우림법 표의 `<th>` 오른쪽 끝에 드래그 핸들이 있어 칸 너비를 직접 조절할 수 있고, 표 id별로 `localStorage`(`jamwon-tea-colw-{표id}`)에 저장돼 새로고침해도 그대로 유지됨(`table-layout:fixed`로 고정, 행이 늘거나 줄어도 — thead는 다시 안 그려지므로 — 너비 안 흐트러짐). 내부 탭이 생기면서 **숨겨진 탭 안의 표는 처음엔 폭이 0으로 측정돼 버리는 문제**가 있었음(그 탭을 처음 열면 칸이 다 쪼그라들어 보임) → `setupColResize`가 폭이 0이면 초기화를 건너뛰고, 그 탭이 실제로 열릴 때(`initTablesIn`, 탭 클릭 핸들러에서 호출) 다시 시도하도록 수정함. **칸 너비 계산 로직이 바뀌어 예전 저장값이 안 맞게 되면** `data-colw-v` 속성으로 그 표만 캐시를 무효화함(`setupColResize`가 `표id:버전`을 키로 씀) — 최적 우림법 표(`brewRefTable`)가 실사례: 원래 "차종"·"최적 우림법" 두 칸짜리로 분리됐는데 CSS가 옛날(블렌딩표 안에 여러 칸과 같이 있던 시절) 값 그대로 "최적 우림법"에 `width:170px` 고정폭을 박아뒀던 탓에, 옆의 "차종"(너비 지정 없음)이 남는 공간을 다 흡수해서 차종만 엄청 넓고 정작 내용 긴 최적 우림법 칸은 좁게 눌려있었음 → 그 170px 고정폭을 없애 내용 기준으로 넓어지게 CSS를 고치고, 이미 사용자 브라우저에 저장돼있을 옛날(나쁜) 폭 값은 `data-colw-v="2"`로 무시하게 함(구매처 연락처 표도 담당자 칸이 새로 생겨 칸 수가 바뀌어서 같이 버전 올림). **저장 안정성**: 입력 후 0.5초 뒤 저장하는 디바운스라 그 사이에 탭을 닫거나 다른 화면으로 전환하면 마지막 입력이 날아갈 수 있어서, ①탭이 백그라운드로 가거나(`visibilitychange`) 페이지를 떠나거나(`pagehide`) 창 포커스를 잃을 때(`blur`) 대기 중인 저장을 즉시 실행하는 `flushAllSaves`, ②그 칸에서 포커스가 빠질 때(`change` 이벤트 — 다른 칸·다른 탭 클릭 등, 디바운스나 창 이탈까지 안 기다림)도 바로 저장하도록 모든 입력칸에 추가함(구매처 비교표/구매처 연락처/다과 단가/다과 상 구성/블렌딩표/운영설정 전부). **저장 상태 표시 통일**(`markSaving`/`markSaved`/`markSaveFailed`): 예전엔 운영설정·블렌딩표 저장만 화면 하단에 "자동 저장됨"이 떴고 나머지 표들은 저장 성공/실패를 전혀 표시하지 않아서, 실제로 저장이 안 되고 있어도 알아챌 방법이 없었음 — 이제 표 하나만 저장해도 그 문구가 같이 갱신됨. **탭을 여러 개 띄워놨을 때 저장이 사라지는 버그 수정(dirty 플래그)**: 같은 브라우저에 이 페이지를 두 개 이상 탭으로 열어두면(북마크+최근탭 등으로 흔히 생김), 아무것도 안 건드린 오래된 탭이 나중에 배경으로 가거나 닫힐 때 `flushAllSaves`가 무조건 전부 다시 저장해버려서, 자기가 불러온 시점의 낡은 값으로 다른 탭이 방금 저장한 내용을 조용히 덮어써 사라지는 버그가 있었음("분명 저장했는데 없어진다" — 다과 단가·구매처 연락처·블렌딩표(비율/구매처/다과상 선택) 등 데이터 종류를 가리지 않고 발생 가능했던 근본 원인). 데이터 종류별로 `compareDirty`/`vendorDirty`/`snackDirty`/`snackSetDirty`/`stateDirty` 플래그를 둬서 그 탭에서 실제로 고친 것만 저장하게 바꿈 — 안 건드린 탭은 절대 남의 최신 데이터를 옛날 값으로 되돌리지 않음(저장 성공 시 해당 플래그 리셋, 실패 시 유지해서 다음 flush 때 재시도). **수동 저장 버튼**(`#manualSaveBtn`, 화면 우하단 고정): 자동저장(디바운스+change+flush)을 여러 번 고쳤는데도 "저장 안 됐다"는 불안이 반복돼서, 지금 이 탭이 가진 모든 표를 dirty 여부와 상관없이 즉시 저장하고 "✅ 저장됨"으로 확인해주는 버튼을 추가함(`saveEverythingNow`) — 자동저장은 그대로 두고 병행. 탭·화면 어디에 있든 fixed로 항상 떠 있고, **Cmd/Ctrl+S**로도 같은 동작(브라우저 기본 "다른 이름으로 저장" 대신 가로챔). **5분마다 추가 자동저장**: 디바운스·change·flush를 전부 놓치는 극단적 상황(한 칸에 커서만 오래 두고 안 움직이는 등)까지 대비한 안전망으로, 이 탭이 화면에 보일 때만(`document.visibilityState==='visible'`) `flushAllSaves`를 한 번 더 돌림 — dirty 체크는 그대로 적용돼서 안 건드린 데이터는 여전히 안 건드림, 숨겨진(백그라운드) 탭에서는 아예 안 돎.
- **데이터**: 현재 빈 상태로 시작. 예전 러버블 CSV(todos 300개)는 원하면 Supabase Table Editor로 Import 가능.

---

## 4. 그림공부 (하루앱 새 탭 + 옵시디언 미러링)

- **뭐냐**: 작가별로 그림(작품)을 쌓아 공부하는 기능. 작가 태그를 달면 그 작가의 모든 그림에 자동 상속(합산)되고, 태그를 누르면 무드보드처럼 관련 그림이 다 모여 보임.
- **어디**: 하루앱(`webapp/index.html`) 탭 `그림공부` (`data-tab="art"`). Supabase가 원본, 옵시디언은 봇이 주기적으로 미러링(한쪽 방향, 옵시디언→하루앱 반영은 안 됨).
- **태그 상속(합산) 원리**: 저장할 땐 작가 태그와 그림 태그를 따로 저장, **보여줄 때만 합쳐서** 표시(`effTags`, JS)/`art_sync._effective_tags`(파이썬) — 그래서 작가 태그를 나중에 바꿔도 소급 적용됨. 자기 태그(✕로 지울 수 있음) vs 상속 태그(그림 화면엔 뜨지만 못 지움, 작가 쪽에서 지워야 함)로 구분해서 보여줌.
- **데이터 모델(Supabase)**: `art_artists`(id,owner,name,tags jsonb,note,created,updated), `art_works`(id,owner,artist_id,title,year,genre,medium,image_url,source_url,tags jsonb,note,favorite,created,updated). id는 프론트에서 생성(`'ar'+uid()`/`'aw'+uid()`)해서 `upsert`로 저장 — todos/memos와 같은 방식. `hasArt` 프로브 플래그로 테이블 없어도 안 깨짐.
- **이미지**: Supabase Storage 버킷 `art`(public read, 본인 폴더에만 업로드 — `storage.foldername(name)[1]=auth.uid()`). 새 그림 폼에서 **파일 선택 · 링크 붙여넣기 · 클립보드 붙여넣기(Cmd/Ctrl+V, `wireImageDropzone`은 아니고 document paste 델리게이션) · 드래그앤드롭**(`#artImgDropzone`, `wireImageDropzone`) 네 가지 다 가능, 다 같은 이미지 링크 인풋(`#artImgLink`)에 채워짐.
- **작가 선택**: 새 그림 폼의 작가 입력은 `<input list>` + `<datalist>`(네이티브 자동완성) — 기존 작가면 그대로 매칭, 없는 이름이면 저장 시 새 작가로 자동 생성(`resolveArtistByName`). **작가·제목 둘 다 비워도 저장 가능**(작가 없으면 미분류, 제목 없으면 "제목 없음") — 사이드바 **"🖼 그림 바로 추가"** 버튼으로 작가 선택 없이 어디서든 그림부터 툭 던질 수 있음(주머니처럼). 미배정 그림은 사이드바 **"🗂 미분류 그림"** 칸(`data-id="__unassigned__"`)에 모이고, 그림 상세화면에서 **작가 필드**(`#artFArtist`, change 이벤트)로 언제든 나중에 배정 가능.
- **UI 구조**: 왼쪽 사이드바(그림 바로 추가 · 작가 검색·목록·미분류 그림 · 전체 태그클라우드) + 메인(작가 화면=태그·공부메모·그림 그리드 / 그림 화면=작가 포함 필드·효과태그·공부메모 / 태그 클릭 시 무드보드 그리드 / 미분류 그리드). 삭제는 실행취소 토스트 방식(다른 삭제와 동일), 작가 삭제 시 그 작가의 그림도 같이 삭제(되돌리기 가능).
- **옵시디언 미러링**(`inbox_bot/art_sync.py`): `Artists/{작가}.md`(frontmatter tags=작가 태그) + `Artworks/{작가} - {제목}.md`(frontmatter tags=효과 태그(합산), `아티스트: [[작가]]` 위키링크로 백링크 연결 — 파일 경로에 타임스탬프 없이 이름 기반 고정이라 재동기화해도 같은 파일을 덮어씀(중복 생성 안 됨), 내용 바뀐 노트만 커밋. 텔레그램 **`그림동기화`** 명령으로 즉시 실행, 또는 20분마다 자동(`_periodic_art_sync`, 조용히·바뀐 게 있을 때만 로그). **env**: `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`/`HARU_OWNER_ID`를 인박스봇 Railway 서비스에도 넣어야 함(일정봇과 같은 값).
- **옛 볼트 노트 가져오기**(`import_artists_from_vault`, 텔레그램 **`작가공부 가져오기`**): 니들보스에서 옮긴 `작가 공부` 폴더(작가별 노트, `# 이름` + `#artist #스타일태그` + 자유 텍스트 형식)를 하루앱 `art_artists`로 1회성 이전. **하위 폴더까지 재귀 탐색**(`_walk_md`, `vault.list_dir`) — 실제 구조가 `작가 공부/고전/…`, `작가 공부/다다,포스트모더니즘,팝/…`처럼 카테고리 폴더 안에 작가 노트가 있어서, **하위 폴더명(쉼표로 여러 개면 각각 분리)도 태그로 흡수**함. `#artist` 태그 없는 파일(README 등)은 자동 제외, 이미 같은 이름 작가 있으면 건너뜀(재실행 안전). **이미지는 안 옮김**(볼트가 Private라 그 안 이미지 URL은 웹앱에서 로그인 없인 못 읽음 → 텍스트만 이전, 대표 이미지는 하루앱에서 수동으로 재업로드 필요).
- **아이패드 손글씨**: 처음엔 옵시디언 Excalidraw 플러그인으로 가려다가, **하루앱 안에 직접 캔버스**로 최종 결정. 그림 상세화면 **"✏️ 손으로 공부하기"** 버튼 → 전체화면 캔버스 모달(`#artSketchModal`) → Pointer Events로 애플펜슬 **압력 감지**(`e.pressure`로 선 굵기), 색상 3종(검정/빨강/파랑)+지우개+실행취소(캔버스 스냅샷 스택, 최대 25단계)+전체지우기. 저장하면 PNG로 캡처(`canvas.toBlob`) → `art` 스토리지 버킷 업로드 → `art_works.sketch_url`에 저장, 그림 화면에 썸네일로 보임, 다시 누르면 기존 그림 위에 이어서 그릴 수 있음(`openSketchPad`가 기존 sketch_url을 캔버스에 먼저 그려줌). `hasSketch` 프로브 플래그로 컬럼 없어도 안 깨짐(SQL 마이그레이션 `haru_art_sketch.sql` 필요).

---

## 배포 / 인프라 메모

- **Railway**: 봇 2개(일정봇 / 인박스봇). Procfile `worker: python start.py`, `BOT_ROLE`로 구분. **웹 푸시 발송하려면 일정봇 서비스에 `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`(+ 이미 있는 `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`/`HARU_OWNER_ID`)를 추가로 넣어야 함** — 안 넣으면 `푸시테스트` 명령이 "웹 푸시가 아직 설정 안 됐어요"라고 안내함.
- **Cloudflare (하루 웹앱 호스팅)**: `haru.lolcv1294.workers.dev`. `main` 브랜치 push → 자동배포. `wrangler.toml`(repo 루트)이 `webapp/` 폴더를 정적 사이트(Workers Static Assets)로 배포(`npx wrangler deploy`, assets-only). **Netlify에서 이사함**(무료 크레딧 소진으로 production deploy 멈춰서). ⚠️ 예전 Netlify는 개발 브랜치를 봤지만 Cloudflare는 `main`을 봄 → 웹앱 배포하려면 `main`에도 push해야 함.
- **Google**: `GOOGLE_CREDENTIALS_JSON`(서비스계정), `GOOGLE_DRIVE_FOLDER_ID`(사진), `SPREADSHEET_ID`.
- **개발 브랜치**: `claude/content-archive-bot-xv3nx` (봇 개발용). 웹앱 변경은 `main`에도 fast-forward push해야 Cloudflare가 배포함.

## 자주 하는 작업 (how-to)

- **웹앱 고치기**: `webapp/index.html` 수정 → 개발 브랜치 + `main` 둘 다 push → Cloudflare 자동 반영 (`haru.lolcv1294.workers.dev`)
- **봇 고치기**: `bot/` 또는 `inbox_bot/` 수정 → push → Railway 자동 재배포
- **메모리 갱신**: 뭔가 구조가 바뀌면 이 `CLAUDE.md`도 같이 고칠 것

## 다음 할 일 (백로그)

1. ~~하루 웹앱 GitHub↔Netlify 자동배포 연결~~ ✅ Cloudflare로 이사 완료 (`main` push→자동배포)
2. 인박스봇 볼트/Railway 연결
3. ~~텔레그램 → 하루 웹앱 주머니로 던지기 연동~~ ✅ 완료 (`앱 <내용>`, `bot/haru_app.py`) — Railway 환경변수만 넣으면 작동
4. 예전 CSV 300개 Supabase로 옮기기 (원할 때)
5. ~~메모 → 옵시디언 실제 내보내기~~ ❌ 보류 결정 — 메모는 하루앱에서만 관리하기로 함(옵시디언 연동 안 함)
6. 월별 업무 로그 탭(하루앱): 완료함+캘린더 자동 요약 + 막힌 것/기억할 만한 것 등 짧은 수동 입력 — 아이디어만, 아직 미착수
7. ~~캘린더 탭 루틴/투두 재구조~~ ✅ 완료 + **실기기 빌드·설치까지 확인됨(2026-09-26)** — 인텔 맥(2020 27" iMac)에서 최신 Xcode(26.5)가 App Store 기준 "M1 이상 필요"로 막혀있었는데, developer.apple.com에서 받은 버전은 실행됨(왜 다른지는 불명, 결과적으로 됨). 빌드 중 겪은 문제들 다 해결: Organization Identifier 누락(Next 비활성화), Signing 팀 미지정, 개발자 모드 꺼짐, "신뢰하지 않는 개발자" 미신뢰, 위젯 스킴으로 직접 실행 시도(SpringBoard 거부 에러) → 메인 앱 스킴으로 바꿔서 해결, Xcode 자동생성 파일 안 지워서 `@main` 중복 컴파일 에러, `containerBackground` API 미채택 경고, `AppIntents` import 누락·`accentColor` 타입 추론 에러. 이후 추가 요청으로 **잠금화면 위젯을 루틴/투두 2개로 분리**(양옆에 나란히 놓게, 기존 통합 위젯은 홈 화면 전용 systemMedium/Large로), **홈 화면 위젯에 아침/점심/저녁 슬롯 구분** 추가. 무료 Apple ID라 **인증서 7일마다 만료**되는 제약은 그대로 있음(유료 계정 $99/년 없으면 주기적 재설치 필요, 아직 유료 전환 안 함). **버그 수정(2026-09-26)**: 잠금화면 루틴 위젯(`HaruRoutineWidget.swift`)이 서버가 준 순서 그대로만 보여줘서 아침/점심/저녁이 섞여 보이던 문제 — 홈 화면 위젯(`HaruWidgetView`)엔 있던 슬롯 정렬 로직이 이 위젯엔 빠져있었음. `slotOrder`(아침→점심→저녁→시간무관) 기준으로 클라이언트에서 다시 정렬하도록 추가.
8. ~~콘텐츠 아카이브(일정봇 → 하루앱 통합) + 카카오/네이버 API 태그 자동화~~ ✅ 완료(2026-09-26, "콘텐츠 아카이브건은 진행합니다") — 자세한 내용은 하루앱 섹션의 "콘텐츠 아카이브 탭" 항목 참고. **API 키 등록은 사용자 몫**: 카카오/네이버 개발자센터에서 앱 등록 후 키를 받아 Supabase 대시보드(Project Settings → Edge Functions → `archive-search` → Secrets)에 `KAKAO_REST_API_KEY` 또는 `NAVER_CLIENT_ID`+`NAVER_CLIENT_SECRET`로 넣어야 자동 채우기가 실제로 동작함(이 세션은 Supabase MCP로 시크릿을 직접 못 넣음). 안 넣어도 아카이브 탭 자체(수기 입력·수정·삭제)는 이미 정상 동작.
